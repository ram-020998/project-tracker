---
inclusion: auto
---

# Jarvis Knowledge Base — T-Retriever Navigation Pattern

## What Is This

The Jarvis Knowledge Base (KB) is a pre-computed intelligence layer that lives entirely inside Appian. A Java plugin parses every object in an application and generates structured JSON documents — app tree, clusters, dependency graph, object content with resolved UUIDs, data model, patterns, architecture, security audit, and dead code analysis.

The KB eliminates the need for repeated live API calls to understand an application. Instead of making 10-20 calls to search, fetch, and trace dependencies, the KB provides the same information in 1-3 calls through a tiered retrieval pattern.

## When to Use KB vs Live API

| Use KB (jarvis_* tools) | Use Live API (existing MCP tools) |
|--------------------------|-----------------------------------|
| Understanding application structure | Creating or deploying objects |
| Researching how a feature works | Evaluating SAIL expressions |
| Finding objects by name, type, tag, or pattern | Getting current constant values |
| Tracing dependencies and impact | Checking real-time data (query_sql) |
| Reading SAIL code with resolved UUIDs | Fetching object diffs (get_object_diff) |
| Exploring data model and relationships | Package operations (inspect, deploy) |
| Identifying dead code or shared utilities | Searching recently created objects not yet in KB |
| Understanding architecture and layers | Writing to the environment |
| Design research — "how is X implemented?" | |
| Code review pre-analysis — understanding context | |

**Rule of thumb:** KB for reading/understanding, live API for writing/current-state.

**Important:** The KB is a snapshot generated by the plugin. Objects created after the last KB generation won't appear. For recently created objects, fall back to live API tools (`search_objects_by_name`, `list_application_objects`, `search_objects_semantic`).

## Resolving kbFolderId

Every KB tool requires `parentFolderId` — the Appian folder ID where the KB documents live.

**Lookup process:**
1. Call `get_jarvis_config` to get the list of registered applications
2. Find the application by matching `appPrefix`, `appName`, or `jiraProjects`
3. Use the `kbFolderId` field from the matched application's `appConfig`

**Example response structure:**
```json
{
  "appConfig": {
    "appPrefix": "AS_VM",
    "kbFolderId": 1510447
  },
  "staleCount": 0
}
```

If `kbFolderId` is null or empty for an application, the KB has not been generated for it yet. Fall back to live API tools.

---

## The T-Retriever Pattern

Navigate the KB in tiers — start broad, drill down only as needed. Each tier adds detail but costs more context.

```
L0: App Tree (shape)     → 1 call, ~2KB
 └─ L1: Cluster / Search → 1 call, ~5-20KB
     └─ L2: Analysis      → 1 call, ~2-10KB
         └─ L3: Content    → 1 call, ~5-50KB per object
```

### L0 — Application Overview

**Tool:** `jarvis_get_app_tree`
**Cost:** 1 call
**Returns:** App metadata, entry points grouped by type (site pages, record actions, web APIs, scheduled processes), type counts, cluster summary with sizes.

**Use when:**
- Starting work on an unfamiliar application
- Need to understand the application shape before diving in
- Looking for the right entry point to investigate
- Need type counts or cluster list

**What you learn:** How big is the app, what are the main features, where are the entry points.

### L1 — Feature Drill-Down

**Tools:** `jarvis_get_cluster`, `jarvis_search_objects`, `jarvis_get_objects_by_type`

| Tool | Use When |
|------|----------|
| `jarvis_get_cluster` | You know which feature/entry point to explore. Use exact cluster name from app-tree. |
| `jarvis_search_objects` | You're looking for something specific — searches name, description, type, and tags. |
| `jarvis_get_objects_by_type` | You want all objects of a type (e.g., all integrations, all record types). |

**Cost:** 1 call each
**Returns:** Object lists with metadata — name, type, description, tags, complexity, cluster membership.

**What you learn:** Which objects are involved in a feature, what they do, how complex they are.

### L2 — Relationship & Analysis

**Tools:** `jarvis_get_dependency_chain`, `jarvis_get_impact_analysis`, `jarvis_get_entry_points_for_object`, `jarvis_get_data_model`, `jarvis_get_shared_objects`

| Tool | Use When |
|------|----------|
| `jarvis_get_dependency_chain` | "What does X call?" / "What calls X?" — focused 1-hop view |
| `jarvis_get_impact_analysis` | "What breaks if I change X?" — full blast radius with transitive callers |
| `jarvis_get_dependents_batch` | "What depends on these 5 groups?" — direct dependents for multiple objects in one call. Use instead of repeated `jarvis_get_impact_analysis` when querying 3+ objects |
| `jarvis_get_precedents_batch` | "What do these interfaces call?" — direct precedents for multiple objects in one call. Use instead of repeated `jarvis_get_dependency_chain` when querying 3+ objects |
| `jarvis_get_entry_points_for_object` | "Which features use X?" — inverse of cluster lookup |
| `jarvis_get_data_model` | "How are records related?" — fields, relationships, data stores |
| `jarvis_get_shared_objects` | "What's shared across features?" — cross-cluster utilities |

**Cost:** 1 call each
**Returns:** Dependency edges, impact trees, data model structure, cross-cluster mappings.

**What you learn:** How objects connect, what the blast radius is, how data flows.

### L3 — Full Object Detail

**Tools:** `jarvis_get_object_content`, `jarvis_get_context`, `jarvis_get_object_xml`

| Tool | Use When |
|------|----------|
| `jarvis_get_object_content` | Need the SAIL code / PM nodes / record config for one object |
| `jarvis_get_context` | Need everything about one object in a single call — content + deps + metadata + impact + clusters |
| `jarvis_get_object_xml` | Need the raw deployable XML for object creation/modification |

**Cost:** 1 call each, but L3 content can be large (5-50KB per object)
**Returns:** Full SAIL code with resolved UUIDs, process model node details, record type fields and relationships, raw XML.

**What you learn:** Exactly how the object is implemented.

**Context budget warning:** L3 content is expensive. Don't fetch more than 3-5 objects at L3 in a single conversation unless necessary. Use L1/L2 to narrow down first.

### Cross-Cutting Analysis (No tier — standalone)

**Tools:** `jarvis_get_patterns`, `jarvis_get_architecture`, `jarvis_get_dead_code`, `jarvis_get_security_audit`

| Tool | Use When |
|------|----------|
| `jarvis_get_patterns` | "Show me CRUD sets, wizard patterns, batch processes" — detected implementation patterns |
| `jarvis_get_architecture` | "What are the application layers?" — layer breakdown, central records, most connected objects |
| `jarvis_get_dead_code` | "What's unused?" — unreachable objects by type |
| `jarvis_get_security_audit` | "Any security issues?" — missing security groups, overly permissive access |

These are application-wide analyses. Call them when the question is about the whole app, not a specific object.

---

## Navigation Recipes

### "How does feature X work?"
```
L0: jarvis_get_app_tree → find the cluster for feature X
L1: jarvis_get_cluster → see all objects in the feature
L3: jarvis_get_object_content → read the key 2-3 objects
```
**Budget:** 3-5 calls

### "What breaks if I change object X?"
```
L2: jarvis_get_impact_analysis → blast radius, affected clusters
L2: jarvis_get_entry_points_for_object → which features are affected
```
**Budget:** 2 calls

### "Show me the data model for evaluations"
```
L2: jarvis_get_data_model → all record types, fields, relationships
```
**Budget:** 1 call

### "What does object X do?"
```
L3: jarvis_get_context → content + deps + metadata + impact in one call
```
**Budget:** 1 call

### "Find objects related to scoring"
```
L1: jarvis_search_objects → search by keyword across name, description, tags
```
**Budget:** 1 call

### "How is the app structured?"
```
L0: jarvis_get_app_tree → overview
Cross: jarvis_get_architecture → layers, central records, connectivity
```
**Budget:** 2 calls

### "What patterns exist in the app?"
```
Cross: jarvis_get_patterns → CRUD sets, wizards, batch processes, utilities
```
**Budget:** 1 call

### "Is object X unused?"
```
L2: jarvis_get_dependency_chain → check if anything calls it
Cross: jarvis_get_dead_code → or check the full dead code report
```

### "Show me dependents for all security groups"
```
L1: jarvis_get_objects_by_type(objectType: "group") → get all group names
L2: jarvis_get_dependents_batch(objectNames: "Group1,Group2,Group3,...") → all dependents in one call
```
**Budget:** 1 call

### "I need to create a new object similar to X"
```
L1: jarvis_search_objects → find similar objects
L3: jarvis_get_object_xml → get the XML template to base the new object on
```
**Budget:** 2 calls

---

## Integration with Existing Workflows

### Knowledge Query Workflow
When the knowledge-query-workflow activates and the target application has a `kbFolderId`:
- **Phase 0 (new):** Check KB first before live API calls
- For Technical/Business/Structural questions → use KB L0-L3 navigation
- For Impact questions → `jarvis_get_impact_analysis` replaces recursive `get_object_dependencies` calls
- For Data Model questions → `jarvis_get_data_model` replaces multiple Record Type fetches
- For Inventory questions → `jarvis_get_objects_by_type` replaces paginated `list_application_objects`
- Fall back to live API only when KB doesn't have the answer (recently created objects, current values)

### Design Document Workflow
When the design-doc-workflow activates and the target application has a `kbFolderId`:
- **Step 3 Track A:** Replace Atlas with KB navigation
  - `jarvis_get_app_tree` → understand application shape
  - `jarvis_search_objects` → find relevant objects (replaces Atlas search_objects)
  - `jarvis_get_cluster` → understand feature context (replaces Atlas get_bundle)
  - `jarvis_get_object_content` → read SAIL code (replaces Atlas get_object)
  - `jarvis_get_patterns` → find similar implementation patterns (replaces Atlas search_bundles)
- **Step 3.5:** `jarvis_get_impact_analysis` can supplement live `get_object_dependencies` for risk assessment
- **Step 3 Track B (live API):** Still needed for recently created objects and database verification

### Code Review Workflow
When the code-review-workflow activates and the target application has a `kbFolderId`:
- **Pre-analysis (new):** Before reviewing individual objects, use KB to understand context:
  - `jarvis_get_cluster` → understand the feature being modified
  - `jarvis_get_dependency_chain` → understand what each object connects to
  - `jarvis_get_impact_analysis` → pre-compute blast radius for modified objects
- **Step 4 (object review):** Still uses live API for source code + diff (KB may be stale)
- KB provides the "why" context that makes review findings more actionable

### Spike Research Workflow
When investigating a topic and the target application has a `kbFolderId`:
- Use KB as the primary codebase research tool
- `jarvis_get_architecture` → understand application layers
- `jarvis_get_patterns` → find existing implementation patterns to reference
- `jarvis_get_data_model` → understand data relationships
- `jarvis_search_objects` → find relevant objects by keyword

---

## Tool Quick Reference

| # | Tool | Params | Returns |
|---|------|--------|---------|
| 1 | `jarvis_get_app_tree` | parentFolderId | App overview, entry points, type counts, cluster list |
| 2 | `jarvis_search_objects` | parentFolderId, query | Matching objects with metadata, tags, complexity |
| 3 | `jarvis_get_cluster` | parentFolderId, clusterName | All objects in a feature cluster |
| 4 | `jarvis_get_object_content` | parentFolderId, objectName | SAIL code with resolved UUIDs, inputs, PM nodes |
| 5 | `jarvis_get_context` | parentFolderId, objectName | Content + deps + metadata + impact (all-in-one) |
| 6 | `jarvis_get_dependency_chain` | parentFolderId, objectName | calls/calledBy for one object |
| 7 | `jarvis_get_impact_analysis` | parentFolderId, objectName | Blast radius: direct + transitive callers, affected clusters |
| 7b | `jarvis_get_dependents_batch` | parentFolderId, objectNames (comma-separated) | Direct dependents for multiple objects in one call |
| 7c | `jarvis_get_precedents_batch` | parentFolderId, objectNames (comma-separated) | Direct precedents for multiple objects in one call |
| 8 | `jarvis_get_data_model` | parentFolderId | Record types, fields, relationships, data stores |
| 9 | `jarvis_get_objects_by_type` | parentFolderId, objectType | All objects of a type with metadata |
| 10 | `jarvis_get_entry_points_for_object` | parentFolderId, objectName | Which clusters/features contain this object |
| 11 | `jarvis_get_patterns` | parentFolderId | CRUD sets, wizards, batch processes, utility libraries |
| 12 | `jarvis_get_architecture` | parentFolderId | Layers, central records, most connected objects |
| 13 | `jarvis_get_dead_code` | parentFolderId | Unreachable objects by type |
| 14 | `jarvis_get_shared_objects` | parentFolderId | Objects appearing in multiple clusters |
| 15 | `jarvis_get_dependency_graph` | parentFolderId | Full dependency graph (all edges) |
| 16 | `jarvis_get_workflow_summary` | parentFolderId, entryPointName | Human-readable workflow/process summary |
| 17 | `jarvis_get_security_audit` | parentFolderId | Security issues: missing groups, overly permissive access |
| 18 | `jarvis_get_object_xml` | parentFolderId, objectName | Raw deployable XML for object creation/modification |
| 19 | `jarvis_validate_deploy` | objectXml | Validates XML before deployment |
