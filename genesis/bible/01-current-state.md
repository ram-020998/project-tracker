<!-- GENESIS BIBLE — CHUNK 01. DO NOT summarize or drop content when editing; keep it verbatim-faithful. -->
> **This file is one chunk of the Genesis bible.** The bible is split across `bible/` and indexed by
> [`../AGENT_ONBOARDING.md`](../AGENT_ONBOARDING.md). **When asked to "read the bible", read the index AND every
> chunk it lists, then follow all of it religiously.** This chunk holds: **§2 Current state — repos/tags table, dependency chain, test counts, milestones, what works today.**
> Section numbers (§0–§10) are the ORIGINAL bible sections and are preserved here; the §→chunk map lives in the index.

---

## 2. Current state (as of genesis v0.66.1)

> **⭐ SHIPPED — Two-toggle collaboration enablement (genesis v0.66.0/v0.66.1, ADR-065):** collaboration is now active only when **BOTH** the env master (`GENESIS_COLLAB_ENABLED`, builds the provider at boot) **AND** a persisted **Settings-level toggle** (`~/.genesis/collab.json` `ui_enabled`, flipped in Settings → Collaboration) are on. The Settings toggle defaults **off** and can only be turned **on** when the **Genesis Hub app is reachable in the dev env** (`GET /collab/availability`; `PUT /collab/config` → 409 otherwise). The **Hub URL is derived from the dev-tagged env** (`{dev.url}/suite/webapi`) — no separate URL setting; the **Hub app UUID** is a one-time static field in Settings. `CollaborationService.is_enabled()` = provider-built AND `ui_enabled` (live — no restart to flip). New: `genesis/collab/config.py` (`CollabConfigStore`, no migration), `GET /collab/availability` + `PUT /collab/config`, extended `GET /collab/config`, and a Settings `Switch` + Hub-app-UUID field (stale "later phase" copy removed). **v0.66.1** fixed the derived Hub base to include `/suite/webapi`. Genesis-only. See `bible/04` ADR-065.

> **⭐ SHIPPED — Phase 38 Collaborative SDLC (genesis v0.65.0, 2026-09-06; implements ADR-063; the FINAL collaboration phase):** the human-authored SDLC entities now sync across a team over the Phase-37 `AppianHubProvider` + `CollaborationService` — **opt-in / off by default** (solo unchanged); genesis-only; **no migration** (m0019 stands). **`genesis/collab/service.py`** gained per-entity mappings: **features + published stage artifacts** (`publish_feature_stage` publish-on-complete — cross-machine parent resolution `feature_id`↔`feature_sync_uuid`; artifact **bytes** as an `artifact` blob; **pull = read-only mirror** to the puller's own disk; `chat_session_id`/`run_id`/`html_path`/`feature_id` **never leave**), **epics/stories** (`publish_feature_backlog` at Finalize + `publish_epic`/`publish_story`/`publish_deletion` tombstones; AC/questions/labels travel as **arrays** [Appian `GH Story Item`] but stay JSON-string columns locally — **lossless**; optional epic parent), and **boards** (`publish_board_move` — lane = the shared story `status`; **auto membership** via `reconcile_board_membership`; in-lane `board_position` + `workbench_boards` curation stay **local**). **`genesis/api/collab_sync.py`** = the publish/pull/advisory/adopt action surface (409/401/503/404 Hub error mapping). Web: **`features/collab/SdlcControls.tsx`** (`<PublishToTeamButton>` publish-on-complete+opt-in gated, `<PublishedBy>`, `<AdvisoryMarker>`) + hooks, mounted on the stage workspace + Workbench board (lane-move auto-publish) + a Settings **Sync-now** + **AdoptionCard** (one-time confirmed **adoption** bulk-publish, idempotent). Live acceptance is user-driven (`scripts/acceptance/phase-38-collaborative-sdlc-acceptance.py` proves it headless). **The collaboration program (35→38) is DELIVERED for the KB + features + stories + boards; shared-memory sync is the one deferred future phase.** See `bible/08` §9 + `progress/phase-38-collaborative-sdlc.md`. **Deferred display niceties** (documented in `specs/phase-38-collaborative-sdlc/38-04-deferred-ui-notes.md`): per-stage attribution + the cross-stage staleness badge mount.


> **⭐ SHIPPED — Phase 37 The Genesis Hub wire-up (genesis v0.64.0, 2026-09-06; implements ADR-063 against ADR-064):** the collaboration mechanism goes **live against the real Appian Hub** for the first two shared surfaces — the **application KB** + **identity/teams** — plus the reusable **sync-UX foundation**. New in `genesis/collab/`: **`AppianHubProvider`** (implements the frozen Phase-36 contract — records CAS→409, blob dedup, changes/activity, teams as generic `records`; `appian-api-key` auth from the ADR-048 dev-env store; offline-first `is_available()`; fixture-tested contract-conformance) + KB **pull-first** sharing (**`KbStore.hydrate_from_blob`** SCD-2 replace-current off-loop; **`kb/blob.py`** gzipped code-free blob, deterministic `mtime=0` so content-hash dedup hits; **`publish_kb`/`pull_kb_if_newer`**; per-app lock; Hub-app exclusion; an off-by-default `kb-hub-pull` scheduler job) + **identity/teams live** (the provider-agnostic `/collab/*` routes now hit Appian) + an **onboarding-before-first-publish gate** (`require_onboarded`/`NotOnboardedError` → 409; attribution carries the canonical username) + the **sync-UX foundation** (`GET /collab/status`+`/changes`; `useHubStatus`/`useHubChanges`/`useSyncNow`; `<HubStatus>` offline-first + `<StalenessBadge>` notify-then-apply). **No migration** (m0019 stands); **genesis-only** (no core/SDK/workflows change). Live acceptance is user-driven (`scripts/acceptance/phase-37-hub-acceptance.py` proves it headless). See `bible/08` §9 + `progress/phase-37-hub-wireup.md`. **Phase 38** wires features/stories/boards publish-pull onto this foundation.


> **⭐ COMPLETE — Phase 36 The Genesis Hub Appian application (ADR-064, 2026-09-05; Appian-side, NO genesis tag):** the dedicated **`Genesis Hub`** Appian app (the ADR-063 Collaboration Hub — the team's Appian **dev env**, no separate Hub env) is fully built + **live-validated** end-to-end with the shared service-account API key. Surface: 11 UUID-keyed record types + 13 relationships + record-level security + 9 constants + a base64 **blob store** (Appian Documents indexed by `GH Blob Version`, dedup + keep-last-N) + **11 Web APIs** (`meta`; `records` upsert[PUT, base-version **CAS→409**]/get/list; `changes`; `activity` set/list/clear; `blobs` put/get/versions) + **12 helper expression rules**. Built by a **separate write-capable Dev-MCP agent** — genesis stays **read-only** against Appian (ADR-036/037); the only genesis-repo footprint is the **frozen contract v1.0.0 + fixtures + runbook** at `specs/phase-36-genesis-hub-appian-app/contract/` (the Phase-37 `AppianHubProvider` interface). See `bible/08` §9 + `progress/phase-36-genesis-hub-appian-app.md`.


> **⭐ SHIPPED — Phase 29 UX Design stage (genesis v0.55.2 + genesis-workflows v0.12.0 + genesis-core v0.9.6 + kiro-agent-sdk v0.7.1, 2026-08-28, CI green):** a feature's **UX Design** stage goes live — upload a mockup **PDF** → the deterministic **`ux-design-analysis`** workflow (PyMuPDF render → per-screen **multimodal** inventory → spec reconcile → **grounding** [genesis-kb structure + appian-dev code] → synthesize → **grounded verify** critic) → a **`StageFinalizer`** opens the bound **`ux_design`** completion chat + moves the stage to in-review → annotatable review + **Mark complete**. Reuses the Spec-page components **generalized** (D0; Spec unchanged); Spec repointed onto the **m0015** `kb_feature_stages`/StageStore (Decision A, data-safe). ADR-057 Accepted. See `bible/08` §9 + `progress/phase-29-ux-design-stage.md`.

> **⭐ SHIPPED — Phase 27 UI/UX Revamp (genesis v0.53.0, 2026-08-24):** a **light-first Indigo·Slate** re-theme + UX
> overhaul of the entire web app (frontend-only; no API/DB/behavioural change) — 27-01..27-11 + user refinements (Home
> dashboard, table/markdown/chat legibility, sidebar to mockup). Single-source `tokens.css` brand knobs (+ Poppins), a
> differentiated app shell (sidebar / glassy TopBar / breadcrumbs / ⌘K), and every surface incl. the tokenized `/memory`
> constellation. Web **vitest 204**; release `fba9a94`, tag `v0.53.0`, CI green (#6641436 master + #6641437 tag). ADR-055
> Accepted (+ ADR-049 amended). See `bible/08` §9 + `progress/phase-27-ui-ux-revamp.md`.

**Five repos** at `/Users/ramaswamy.u/repo-gitlab/ramaswamy.u/`, all pushed to
`git@gitlab.appian-stratus.com:ramaswamy.u/<repo>.git` (the 5th, `genesis-appian-parser`, is new in Phase 16):

| Repo | Tag | Branch | Role |
|---|---|---|---|
| `kiro-agent-sdk` | **v0.7.1** | main | ACP adapter; `collect`/`collect_streaming`; `permission_mode`(`auto_approve`/`auto_deny`/**`ask`**)+`allow_fs_write`; **per-turn credit metering (11-01)**; **interactive permission bridge `permission_mode="ask"`+`on_permission` callback (13-01)**; **`fs_write_root` sandbox for agent file writes (14-05)**; **ACP extensions (Phase 21-04): `SystemInit` exposes models/modes/commands (from `session/new` + `_kiro.dev/commands/available`); `set_model`/`set_mode`; `execute_command` (via a shared `_turn`); `prompt(images)` gated on `promptCapabilities.image`; `on_commands`/`on_session_status` callbacks**; **v0.7.1 (Phase 29): additive `images` kwarg on `query`/`collect`/`collect_streaming` → `client.prompt(images=)` (passed only when present; gated on `promptCapabilities.image`)** |
| `genesis-core` | **v0.9.6** | master | nodes/state/registries/validators; two-tier MCP/CLI registry + introspection (ADR-029); session tool-output store (Phase 9); telemetry carries **metered credits** (Phase 11); `CORE_MAJOR=1`; **`McpRegistry` managed-native `launch_provider` — a `"managed":"<id>"` entry resolves command/args from a local install; introspect 8 MiB stream limit (16-08)**; **`CliRegistry` managed-native launch resolution — the CLI analog, injected `launch_provider` (Phase 19, ADR-040)**; **pins kiro-agent-sdk v0.7.0 (Phase-21 ACP extensions; pin-only bump, `CORE_MAJOR` unchanged)**; **v0.9.4 (Phase 25): `util/atomic_json.py` shared atomic-write/`path_lock` helper (25-03) + `agents/` `AgentProvider` Protocol + `KiroAcpProvider` over the workflow-agent turn path (25-05, ADR-051) — both additive, `CORE_MAJOR` still 1**; **v0.9.5 (Phase 25-10): `nodes/reliable.py` `reliable_agent_step()`/`add_reliable_step()` — compose the ADR-011 reliability trio in one call (additive)**; **v0.9.6 (Phase 29): `kiro_node(image_docs=…)` → base64 ACP image parts + `AgentProvider`/`KiroAcpProvider` `images` (additive; `CORE_MAJOR` still 1)** |
| `genesis` | **v0.66.1** | master | runtime, dist, config, runs, **db (m0001–m0015)**, api (`/api`+SPA), cli, web SPA; **Chat** (Phase 10); **credit tracking** (Phase 11); worker loop `recursion_limit` (12-01); **Copilot (Phase 13-01..06)**; **Skills (Phase 14-01..05)**; **run-launch file attachments (ADR-035, Phase 15-01)**; **internalized Appian KB: m0007 `kb_*` + `genesis/kb/KbStore` (16-02)**; **read-only `genesis-kb` MCP server (`genesis/mcp/kb_server.py`) + chat cutover off `appian-atlas` (16-05); `KbStore` UUID-dedupe (16-07); native-MCP bundle controls moved into the MCP detail page + Applications UI polish + `scripts/genesisctl.sh` (v0.34.0)**; **Phase-17 Business Map backend: m0008 `kb_business_maps` + `KbStore.build_evidence_pack` (code-free KB→business evidence pack) + Business Map API (v0.35.0) + Business Map web view (React Flow A value-stream + B capability-constellation, first tab, 17-05, v0.36.0) + readability + click-for-detail popups + radial capability constellation + smoothstep edges (v0.37.0/v0.38.0)**; **pins `genesis-appian-parser`; `build_context` injects `ctx.extras['kb_store']`; checkpointer WAL+busy_timeout (16-03)**; **dev-environment `is_dev` toggle + `dev_environment()` (16-08 §2.0)**; **managed-native MCP installer `genesis/mcp/native/` + `api/native_mcp.py` + `genesis mcp` CLI + Settings→MCP panel (16-08 Stage B)**; **Applications surface: `api/applications.py` + `kb/dev_mcp.py` (Dev-MCP `listApplications`) + `web/features/applications` (16-04)**; **Document Library (Phase 19, ADR-040/041, v0.44.0): m0009 (`kb_documents`/`_links`/`_sections`) + `DocumentStore` + `kb/doc_parsing` (pypdf/python-docx/openpyxl, pinned) + `DocumentSyncEngine` + managed-native `gws` connector (`integrations/gws/`, `cli_tools/native/`, `api/native_cli.py`) + `api/documents.py` + `genesis-kb` doc tools + `build_evidence_pack` docs + `web/features/library` (Document Library page + full-screen viewer + per-app Business Artifacts tab + Settings→CLI connector card)**; **Features & Spec authoring (Phase 20, ADR-042/043, v0.45.0): m0010 (`kb_features`/`kb_feature_specs`/`kb_feature_spec_revisions`) + `FeatureStore` + `api/features.py` (feature CRUD + spec create/context/milestone/status + artifact/sdk.js/export.md) + Chat `feature_spec` mode + vendored MIT Lavish annotation SDK (`api/assets/lavish/`, Genesis-themed) + `markdownify` HTML→MD + `web/features/features` (Features tab + feature page + `SpecWorkspace`)**; **Feature Workspace, Spec-Builder UX & Chat Parity (Phase 21, ADR-044/045, v0.46.0): the feature page is an artifact **pipeline** (`web/features/features/ArtifactPipeline` — Spec Edit/View + Design/Breakdown placeholders; `artifact?annotate=0` read-only preview; builder split to `…/features/:id/spec`); spec builder = full-width `ChatThread` (`chrome="spec"`) + full-screen annotatable Preview (our comment-queue + Send-all); `feature_spec` excluded from the main chat list; **Kiro CLI/ACP parity** (m0011 `chat_sessions.model`; `ChatManager` model-at-creation + `ensure_agent_catalog` + slash-`execute_command` routing + clear/compact + images; `api/chat` `/chat/{models,commands}` + `…/{model,clear,compact}` + `SendMessage.images`; `Composer` parity toolbar + Commands palette + context meter) both places; `chat/export.py` + `…/export.md` Markdown transcript**; **Distribution & browser-based shipping (Phase 22, ADR-046, v0.47.0): `scripts/install.sh` (prereq + SSH-access preflight → clone → venv → `pip install .` → `db upgrade` → writes `~/.genesis/dist.json`) + `runtime/launcher.py` (`genesis up/down/status/logs` — background serve + health-wait + open browser; `genesisctl.sh` is a thin wrapper) + `runtime/updater.py` (`genesis update` + one-click in-app apply: highest `vX.Y.Z` tag vs deployed → checkout → `pip install .` → `db upgrade` → detached restart) + `runtime/kiro_auth.py` (in-app kiro-cli whoami/pty-device-login/logout) + `runtime/preflight.py` (first-run readiness) + `api/system.py` (`/system/update`,`/system/kiro*`,`/system/preflight`) + `web/features/system` (UpdateBanner + PreflightChecklist in AppShell) + Settings→General Kiro sign-in + `docs/INSTALL.md`; tracked branch = `master`; a `clean-install` CI job**; **Scheduled & full-package syncs (Phase 23, ADR-047, v0.48.0): the app sync is re-runnable as a **full-package refresh** — `api/applications.py` unblocks `sync-application` `mode=delta` (auto-pick baseline↔delta, `refresh` alias) + a per-app already-running 409 guard + a web **Refresh** action; a backend **scheduler** (`runtime/scheduler.py` minute-tick + pure `due_slot`; `runtime/schedule_store.py` over **m0012 `scheduled_jobs`**; `runtime/sync_jobs.py`) runs **`application-sync`** (all tracked apps, 07:00 IST weekdays, **serialized** — Appian export is one-at-a-time/409) + **`document-library-sync`** (`scope=library`, 08/12/16/20 IST weekdays), TZ/weekday/daytime-aware + restart-safe, preflight-skips (no dev env / no gws); read-only `GET /api/system/schedules` (`api/schedules.py`)**; **v0.48.1: updater **dev-guard** — `is_managed_install` (a real deployment has `~/.genesis/dist.json`); in a dev/editable checkout `check()` returns `managed=False`+`update_available=False` (banner hidden) and `apply()` refuses, so the one-click update can't `pip install .` over the editable installs**; **v0.48.2: Dev-MCP app enumeration + SPA packaging fixes — `kb/dev_mcp` surfaces `isError` (HTTP 401 etc.) as a real reason (not a silent empty list) + **paginates** `listApplications` (was capped at the server default limit=50); `LCP_API_PATH` is a per-dev-env **persisted** field (`config/environments` defaults it) not a hardcoded literal; **`api/app._resolve_web_static` + pyproject `force-include web/static → genesis/web_static`** so the wheel ships the UI (fixed `/` 404 on a real install), clean-install CI now asserts `/` serves the SPA**; **v0.48.3: the `application-sync` scheduled job ships **disabled by default** (on-demand refresh unaffected; re-enable later)**; **v0.48.4: one-click Update upgrades the WHOLE stack — `updater.apply()` also runs `genesis install` (refresh genesis-workflows); the pinned genesis-core/kiro-agent-sdk/genesis-appian-parser are already re-pulled by `pip install .` (git+ssh tag pins), so a single click updates genesis + core + sdk + parser + workflows to the genesis release's pinned set**; **v0.48.5 (Phase 24-01, ADR-048): environment-scoped credentials for the two core Appian MCPs — creds (`LCP_USERNAME`/`LCP_PASSWORD`/`APPIAN_API_KEY`) are entered on the environment form + stored per-env in the SecretProvider (`env-<hash(label)>` scope, never in environments.json) + resolved ONLY from the dev-tagged env via `EnvironmentRegistry.resolve_var` (secrets injected into the registry at worker/chat/cards sites); `LCP_API_PATH` is a public env field; `LCP_URL`/`APPIAN_DOMAIN` auto-derive from the env URL; the two servers no longer render an MCP config card (`fields.ENV_MANAGED_SERVERS`) + a detail pointer to Settings → Environments; one-time non-destructive migration relocates legacy server-scoped creds into the dev env scope**; **v0.48.6 (Phase 24-02, ADR-049): Applications-first IA — primary nav = Applications · Chat · Runs · Documents (`/` redirects to `/applications`); Overview (metrics) + Catalog (Workflows|Skills) moved into Settings as the first two tabs (default Overview) via extracted `OverviewSection`/`CatalogSection`; standalone `/catalog[/:id/launch]` + `OverviewPage` retained for deep links**; **Architectural Foundation Hardening (Phase 25-01..25-08, genesis v0.49.0 + genesis-core v0.9.4; ADR-050/051 Accepted): typed SDLC `genesis/domain/` (enums/entities/transitions/lifecycle/events/errors) + single-authority `LifecycleService` + **m0013** `lifecycle_transitions` audit + `LifecycleAuditStore` + spec action API `POST /features/{id}/spec/actions/{action}` (illegal/precondition → 409) + web allowed-action buttons (25-01, ADR-050); zero-dep structured logging + `contextvars` correlation IDs `genesis/runtime/logging.py` wired into create_app/worker/CLI (25-02); atomic+serialized JSON writes (environments/secrets/dist + custom MCP/CLI) via `genesis_core/util/atomic_json.py` (25-03); network-exposure guardrail — `serve`/`up` refuse a non-loopback bind without `--i-understand-no-auth`/`GENESIS_ALLOW_NON_LOOPBACK=1` (25-04); `AgentProvider` via genesis-core (25-05, ADR-051); god-module decomposition — `KbStore` 1373→652 (four read mixins in `genesis/kb/_*_mixin.py`; SCD-2 write path kept) + `api/app.py` 841→189 pure composition root (`api/{config,run,catalog}_routes.py` + `api/_shared.py`) + `genesis/services/ApplicationSyncService` de-dups API↔scheduler (25-06); `ChatModeProfile` composes chat modes `genesis/chat/mode_profile.py` (25-07); optimistic concurrency — **m0014** `row_version` CAS on features/specs + `StaleWriteError`→409 + `LifecycleService` state-CAS + run-start idempotency key (25-08). **25-09..25-13 shipped v0.50.0 + genesis-core v0.9.5: DocumentProvider interface + `GoogleDriveProvider` (`integrations/documents/`, 25-09, ADR-052); `reliable_agent_step()` helper (25-10, genesis-core; workflow adoption deferred/opportunistic); observability — `api/metrics.py` `GET /system/metrics` + `GET /runs/{id}/provenance` + `GET /features/{id}/activity` lifecycle feed + web Activity/Metrics views (25-13, §22); Overview trimmed (Active-runs + Installed-workflows sections removed).** Phase 25 COMPLETE (ADR-050/051/052 Accepted); 25-11 (per-story execution) + 25-12 (AI cost pass) in backlog**; **v0.50.1: `GwsClient` export/download run gws from the output's **parent dir** with a relative `-o` (gws 0.22.5 confines `--output` to the process cwd — a live `sync-documents` fetch fix, §7)**; **v0.50.2: the full-screen document viewer (`/documents/:id`) gets a **Rendered | Source** toggle — large docs (`content_md` > 200k chars, e.g. a spreadsheet-derived doc) default to **Source** (raw Markdown in a plain `<pre>` via `CodeBlock`) so they open instantly instead of freezing react-markdown; small docs still default to Rendered (§7)**; **v0.51.1: per-sheet **spreadsheet viewer** — the document viewer gains a **Sheets** mode (default for docs with a `tables_path`) that renders the parser's `tables.json` as an Excel-like **sheet-tab strip + paged grid** (`GET /documents/{id}/tables` served lazily; `web/features/library/SpreadsheetView` + pure `spreadsheet.ts`; column letters + row numbers + 100-row paging + horizontal scroll, no header assumption) — multi-tab workbooks show tab-by-tab instead of one flattened Markdown page**; **v0.51.2: the document viewer's **Rendered/Source/Sheets toggle was removed** — the renderer is now **auto-selected by document type** (tabular→Sheets grid; else Rendered Markdown; a very large non-tabular doc→raw Source as an invisible hang safety net)**; **v0.51.3: **branding** — a `GenesisLogo` SVG mark (v0.51.4: a modern **G monogram** — blue→purple gradient tile + geometric white G; `shared/ui/logo.tsx`) used in the Sidebar; an inline **SVG data-URI favicon** + apple-touch-icon + theme-color + title "Genesis" in `index.html`; **app-wide loading animation** — a pre-JS **boot splash** inside `#root` (animated mark, replaced on React mount) + a branded `LoadingScreen` (RouterProvider Suspense fallback); the loader is a standard **spinner ring** around the static mark (`.genesis-spinner`, v0.51.4 — replaced the spinning orbit), reduced-motion-safe**; **Agentic Memory Layer (Phase 26, ADR-053/054, v0.52.0): a SEPARATE `~/.genesis/memory.db` (`genesis/memory/migrations/mm0001` — bi-temporal `memory_entities`/`memory_relationships`/`memories`/`memory_links`/`memory_communities` graph + FTS5 + `sqlite-vec`) behind DB-agnostic seams (`genesis/memory/` `MemoryStore`/`GraphStore`/`Embedder`[model2vec/fastembed/Null]/`VectorIndex`/`retrieval`); the read-only **`genesis-memory` MCP** (`genesis/mcp/memory_server.py`) chat-wired (`chat/mcp.py` + steering) — hybrid retrieval (semantic+keyword+entity/graph × recency × importance); `chat_read` accessor + `build_context` `ctx.extras` (memory_store/chat_read/embedder_factory); browser-only **curation API** `api/memory.py` + the **/memory** web workspace (graph/list/inspector/review, `web/features/memory`); two scheduler jobs `runtime/memory_jobs.py` (consolidation nightly / maintenance weekly) + `memory_owner_username` + `GET /api/system/memory`**; **v0.52.1: the `/memory` graph view is a dark **d3-force constellation** (`web/features/memory/MemoryGraph` + pure `graph.ts` — entity nodes shaped/coloured by kind + sized by degree; hover-highlight-connected + dim; drag-to-pin; zoom/pan; kind legend) replacing the React Flow radial layout; adds `d3-force`/`d3-zoom`/`d3-selection`/`d3-drag` web deps**; **Feature Workspace framework (Phase 28, ADR-056, v0.54.0): `web/features/features` reframed to a **parallel plug-in workspace** — `stages.ts` (self-describing `StageDescriptor` + derive fns) + `stage-registry.tsx` (plug-in point) + `FeaturePage` (command-center Overview + peer stage cards + ARIA tabs) + `StageWorkspacePage` (routed `…/:stage`) + `SpecCardActions` + `StageWorkspaceHeader`; replaced `ArtifactPipeline`** ; **UX Design stage (Phase 29, ADR-057, v0.55.0): `kb/pdf_render.py` (PyMuPDF==1.28.2) + **m0015** `kb_feature_stages`/`_revisions` + `kb/stages.py` StageStore (Spec **repointed**, Decision A) + `domain` `build_stage_lifecycle` + `chat/mode_profile` `ux_design` + `chat/stage_finalizer.py` StageFinalizer (run→stage bridge) + generalized `api/features.py` `/features/{id}/stages/{stage}` API + `runs/worker.py` resolves the internal managed `genesis-kb` + web `StageArtifactWorkspace`/`AnnotatablePreviewDialog`/`StageBuilderPage`/`UxCardActions` (D0 reuse; `STAGE_DEFS.ux` live)**; **v0.55.1 (live fixes): StageBuilderPage gates the workspace on a BOUND completion chat (a bare draft ux row shows Upload / "analysis in progress", not a dead "no chat session" pane); ADR-035 upload allowlist gains `.pdf` + cap 10→25 MB so the mockup provisions; **v0.55.2 (live-run hardening): the StageFinalizer LOGS finalize failures (was silently swallowed) + self-heals a stranded UX stage on the feature/stage read (`reconcile_stage`) when the live run.final was missed (orphaned worker / app bounce), so opening the stage recovers it without a restart**; **v0.56.0 (minor): global **⌘K search** (`api/search.py` `GET /search` → typed navigable hits across applications/features/documents; `web/lib/api/search.ts` + `CommandPalette` entity groups); run-detail **node status is authoritative from `/steps`** (fixes a stuck "Running" on >1000-event runs — the `/events` cap truncated the fold) + **per-node execution counts** (`fold_steps` `executions`) + **"Round k of N"** conversation dividers + loop re-entry re-shows "running"; the run graph is a **bordered dotted canvas**; the stage workspace drops the redundant "Back to feature" (breadcrumb is the sole nav; a generic `:stage` breadcrumb route makes the feature name a crumb in every stage) + the UX stage empty/in-progress states match the mockup (dropzone + pipeline; animated running)**; **v0.56.1: wired the UX **Re-upload & re-run** control (D11) — a shared `ReuploadMockupButton` (pick a new PDF → confirm → POST `/stages/ux_design/reupload` → `reset_for_reupload` + relaunch) in the stage workspace header + the Overview card; the backend endpoint + `useUploadMockup(reupload)` hook already existed but had no UI trigger**; **v0.56.2: StageFinalizer only finalizes a stage from its **currently-bound run** — so a re-upload's new analysis isn't clobbered by the reconcile sweep re-finalizing the OLD (superseded) done run**; **v0.57.0 (Phase 30 — Technical Design stage, ADR-058 + the ADR-056 prerequisite amendment): JSON `/features/{id}/stages/technical_design/start`+`/rerun` (optional comment, no file) gated on Spec+UX present (409) + `_launch_td`; the **generalized `StageFinalizer`** (workflow→stage `_BINDINGS` registry serving ux-design-analysis + technical-design-analysis; the bound-run + stage-match + idempotency guards preserved); the **`technical_design` `ChatModeProfile`** + `_STEERING_TD`; web `STAGE_DEFS.design` live + `requires:[spec,ux]` + `deriveAvailability` gating + the comment/Start entry + `TechnicalDesignCardActions`/`RerunTechnicalDesignButton`; **UX refinements: clickable stage cards + openable artifacts** (30-05); no migration (m0015 reused)**; **v0.58.0 (post-Phase-30 fixes): run-detail workflow-graph REVAMP — **elkjs** layered LR layout + orthogonal edge routing (back-edges pre-reversed so validator/gate layering stays correct; custom ELK-routed edges so no line cuts through a node), the EXECUTED path drawn **green** with per-edge **×N** traversal counts (new `GET /runs/{id}/transitions` + `steps.fold_transitions`), minimap removed, idle edges recoloured to a visible slate; run-detail **perf** — the Inspector fetches only the selected node's events (`/events?node=`), the graph derives status from `/steps` (no per-SSE-tick fold of the whole event log) + SSE streams only while active + refreshes `/steps` on transitions (fixes graph lag/stall on large agent-heavy runs); **honest partial credit provenance** (a run whose agent turn ran but reported no metering — e.g. a timed-out turn — is flagged, not shown as complete; ADR-032); `runs/worker._snapshot` reports a terminal `done` for a completed graph (no next / no interrupt) so an **approved escalation** finalizes instead of stranding the run 'running'; `index.html` served **`Cache-Control: no-cache`** so reloads always pick up the latest hashed bundle**; **v0.59.0 (Phase 31 — Feature Breakdown stage, ADR-059): the FOURTH stage goes live — multipart `POST /features/{id}/stages/breakdown/start`+`/rerun` (optional notes + up to 3 supporting docs, ADR-035) gated on Spec + UX + Technical Design present (three-way `_fb_prereqs_ready` → 409); `_launch_breakdown` (snapshots the 3 artifacts + attaches the uploads); the **`feature_breakdown` `ChatModeProfile`** + `_STEERING_FB` (keeps the embedded backlog JSON in sync); the StageFinalizer `_BINDINGS` += `feature-breakdown-analysis` (stage `breakdown`, artifact `breakdown.html`); a **Jira-importable CSV export** `GET /features/{id}/stages/breakdown/export.csv` (deterministic from the embedded canonical backlog JSON — `_parse_embedded_backlog`/`_backlog_to_jira_rows`; Issue ID → Parent ID, **create-new-epics**, no custom-field columns; stdlib `csv`, no new dep); web `STAGE_DEFS.breakdown` live + `requires:[spec,ux,design]` + a notes/dropzone entry state + `BreakdownCardActions`/`RerunBreakdownButton` + an Export button; no migration (m0015 reused)**; **v0.59.1 (patch): fixed the Feature Breakdown **Export** crash — `<Button asChild>` wraps its child in a Radix `Slot` but `Button` always renders a `{loading && <Loader2/>}` sibling, so `React.Children.only` threw when the in-review card rendered Export; now a plain download `Button` (+ a regression test; §7)**; **v0.60.0 (Phase 32 — Finalize Stories, ADR-060): once the Feature Breakdown stage is `completed`, a one-time, irreversible **Finalize Stories** action parses the canonical backlog embedded in `breakdown.html` and persists every epic + story/task into new **`kb_epics`/`kb_stories`** (m0016; `current_version` 15→16; `kb_features.stories_finalized_at` one-time marker) via `kb/stories.py::StoryStore`; API `POST /features/{id}/stages/breakdown/finalize` (409 unless completed + not finalized; 422 on empty) + story CRUD (`GET/POST /features/{id}/stories`, `GET/PATCH/DELETE /stories/{story_id}`; `row_version` CAS→409); domain `Story`/`Epic` promoted to live; the reserved **Stories tab** becomes a filterable/paginated grid (Epic·Type·Title, colored tags, grouped by epic) + a routed `StoryDetailPage` (read/Edit/Delete; parent-epic dropdown); a themed `shared/ui` **`Select`** (Radix DropdownMenu) replaces native selects in the Stories + Runs filters; + run-detail graph UX fixes (live executed path + refresh control, readable ×N, standard icons, path legend)**; **v0.61.0 (Phase 33 — The Workbench, ADR-061): a new top-level **Workbench** nav surface holding one **Jira-style Kanban board per application** over the app's finalized stories (aggregated across its features), eight fixed lanes (To Do → Design → Design Review → Implementation → Code Review → Verification → Deployment → Done); a card's lane IS its `kb_stories.status`. Boards are **curated** — added/removed as **left-sidebar sub-tabs** under Workbench (a `+` beside the label adds applications). Stories are **imported in batches** (→ To Do); cards **drag across lanes AND reorder within a lane** (`@dnd-kit` multiple-containers) persisted via a bulk **`PATCH …/lanes/{lane}`** reorder endpoint (sets each card's lane=status + board_position); a card **drawer** (reuses the Phase-32 detail) + **remove-from-board**. Backend **m0017** (`workbench_boards` + `kb_board_cards`; `current_version` 16→17) + `kb/boards.py::BoardStore` + `api/workbench.py`; `LifecycleState` += `to-do`/`design-review` + a `STORY_LANES` constant. **UI-only board this phase — per-lane automation is a future phase.** genesis-only + m0017 + `@dnd-kit`**; **v0.62.0 (Phase 34 — Design Lane Automation, ADR-062): the FIRST Workbench per-lane automation. Dragging a ticket into **Design** opens a confirm dialog → **Yes** moves it + launches (or re-runs in place) a grounded **`story-design-analysis`** run; **No** just moves it. The run writes an **object-level, code-level** `design.html` (one section per object, NEW/UPDATE tag + the ACTUAL code; process models node-by-node) — gather ticket research → per-object grounded design loop (@genesis-kb + @appian-dev, read-only) → deterministic assemble → grounded verify critic → present; reliability trio per agent. A **running** card is locked + shows a run link; a **failed** run paints the card light-red. On success the card **auto-advances to Design Review** via an **audited** `LifecycleService` transition (m0013 — the first). A **Design Review** card opens a full-page workspace (reused `ChatThread` + annotatable **Lavish** preview → the agent revises `design.html`). Backend **m0018** (`kb_story_stages` + `kb_story_stage_revisions`; `current_version` 17→18) + `kb/story_stages.py::StoryStageStore` + `POST /api/workbench/boards/{app}/cards/{story_id}/design/start` (fail-fast 409 on no dev env / app not synced / workflow not installed) + `chat/story_design_finalizer.py::StoryDesignFinalizer` (bound-run guard + idempotency + on-read recovery) + a `story_design` chat mode; the board-card DTO gains `design_run_status` (from the durable runs table). A pointer-first board collision (post-build fix) so drops land on every lane. **Removed the superseded Phase-15 `design-doc`.** genesis + genesis-workflows (no core/SDK); + m0018** ; **v0.63.0 (Phase 35 — Collaboration Foundations & Identity, ADR-063): the local-first → shared-Hub substrate, opt-in/off-by-default (solo unchanged). **m0019** (`current_version` 18→19): `sync_uuid` (+ backfill + UNIQUE) on the 6 synced tables (kb_features/kb_feature_stages/kb_epics/kb_stories/workbench_boards/kb_board_cards) + `row_version` where missing + owner/team/provenance cols + `upstream_versions_json` on the stage tables + `collab_identity`/`collab_teams`/`collab_memberships`/`collab_sync_state` local tables (additive + idempotent). **`genesis/collab/`** — the pluggable **`SyncProvider`** Protocol + DTOs + file-backed **`LocalHubProvider`** (base-version CAS→`HubConflict`, content-hash blob dedup, change manifest, advisory TTL, teams) + **`CollaborationService`** (publish/pull + provenance + a `_LOCAL_ONLY` no-leak payload guarantee) + **`build_sync_provider()`** opt-in registry; **`identity.py`** self-asserted identity + **`canonical_username()`** threaded into every `LifecycleService.transition(actor=)` site + the `memory_owner_username` consumers. **`api/collab.py`** (identity/teams/config) + a preflight item + a Settings→**Collaboration** page + a first-run **OnboardingDialog** (both gated on the opt-in flag). genesis-only** |
| `genesis-workflows` | **v0.16.0** | master | registries (incl. `jarvis`+`jira`+`appian-atlas` + **`appian-dev`/`appian-devops`** now **managed-native refs** with read/export-only allowlists set from the real installed `tools/list` — 16-08 Stage B; **`appian-dev` injects `USERNAME`/`PASSWORD` from the same `LCP_*` secrets + a templated `${LCP_API_PATH}`, no `LCP_AUTH_METHOD` — v0.9.4, matches current lcp-mcp-server builds**; **+ `gws` managed-native read-only CLI entry, Phase 19**), steering, `hello-appian` + `erd-generation` + `code-review` + `design-doc` + **`sync-application` (Appian KB baseline sync via Deployment REST → parser → KbStore, Phase 16-03)** + **`generate-business-map` (Phase-17 agent-synthesized Business Map from the code-free KB, v0.9.0)** + **`sync-documents` (Phase-19 Google-Drive→Document-Library pull/parse/store via the read-only gws connector, program-only)**; `skills/` library + `skills-registry.json` + `ci/validate_skills.py` gate; seed `gam` skill (Phase 14-02); **v0.9.5: `appian-dev`/`appian-devops` `secretKeys`/`publicKeys` emptied — their creds are configured per-environment (Phase 24-01, ADR-048), not on the MCP card; env maps unchanged**; **v0.9.6: `sync-application` v0.2.3 resolves `APPIAN_API_KEY` from the **dev-tagged env** via `ctx.environments.resolve_var` (ADR-048) with a legacy `appian-devops`/`global` secret fallback — fixes the app↔workflow version skew that failed baseline export with "APPIAN_API_KEY secret is not set" (§7)**; **Phase 26 (v0.10.0): + `memory-consolidation` (nightly memory WRITE path — extract → reconcile ADD/UPDATE/INVALIDATE/NOOP → embed) + `memory-maintenance` (weekly "dreaming" refiner — reflect/dedup-merge/invalidate/decay/recompute-communities) workflows + `registry.json` entries; pins genesis@v0.52.0 (+ genesis-core@v0.9.5)**; **v0.11.0 (Phase 28): + 2 library skills — `appian-object-generation` + `sail-mockup-generation` (validate_skills = 3 skills)**; **v0.12.0 (Phase 29): + the `ux-design-analysis` workflow (9-node grounded mockup→analysis + D2 validators + grounded verify critic) + a managed `genesis-kb` mcp-registry entry; re-pin genesis-core v0.9.6 + genesis v0.55.0**; **v0.12.1 (Phase 29 FB-4): `ux-design-analysis` v0.1.1 reframed to a UX/business deliverable — per screen Mockup intent → Spec basis → **What exists today** → **What changes** (user POV) → Ripple effects → Open Questions; the grounding output is plain-language what-exists/what-changes (**no object names/UUIDs/add-modify verbs**) and the verify critic flags technical leakage; re-pin genesis v0.56.0**; **v0.13.0 (Phase 30): + the `technical-design-analysis` workflow — plan by **functional workstream** → per-workstream **existing-state grounding** (`@genesis-kb` structure + `@appian-dev` code, read-only) → per-workstream **design drafting** → **agent `assemble`** (coherence) → **grounded `verify`** critic (bounded → escalate) → present; two queue-pop loops with per-iteration retry reset; object-level + code-grounded, read-only against Appian; re-pin genesis v0.57.0 (genesis-core pin stays v0.9.6)**; **v0.14.0 (post-Phase-30): `technical-design-analysis` **v0.2.0** — the single big-emission `assemble` agent turn (which hit `turn_timeout` on a ~12-workstream doc → truncated HTML + no metering) split into a bounded **`synthesize`** agent (cross-cutting Overview + Complex Designs only) + a **DETERMINISTIC program `assemble`** that stitches the already-validated per-workstream blocks (each exactly once, in build order) + a programmatically-consolidated Open Questions; stronger `check_design` (require an Open-questions subsection) + `check_doc` structural invariants + new `check_synthesis`; a **`cleanup`** program node on both terminal paths deletes ONLY the tool-output scratch (`_toolcalls/` + saved dumps), preserving design artifacts; re-pin genesis v0.58.0**; **v0.15.0 (Phase 31): + the `feature-breakdown-analysis` workflow — `plan_epics` (epics = the Technical Design's **functional workstreams**, 1:1) → a per-epic **`break_epic`** loop (Appian rules: a form → one story [split by section only when complex], a process model → a separate story, split by **entry point**; **Story** = front-end-testable [**Gherkin** Given/When/Then AC] / **Task** = a backend/data-model/appref/process-only change not FE-verifiable [AC optional]; a one-line TD-anchored `devNoteRef`) → a **DETERMINISTIC program `assemble`** (builds a canonical `backlog.json` + renders a **Lavish-safe** `breakdown.html`: a summary header + `<details>` story cards grouped by epic + a **CSS-only** card/table toggle + the canonical backlog embedded in `<script id=genesis-backlog>` for the Jira export) → a **grounded coverage `verify`** critic (bounded → escalate) → present → cleanup; read-only genesis-kb + appian-dev (light spot-check); re-pin genesis v0.59.0**; **v0.16.0 (Phase 34): + the **`story-design-analysis`** ("Appian Ticket Design") workflow — turn ONE Workbench ticket into an object-level, code-grounded design (HTML): resolve_inputs (fail-fast: dev env + app synced) → load_inputs (Spec/UX/Technical-Design + the story) → gather_context (ticket-relevant research) → plan_objects → a per-object grounded design loop (`design_object` @genesis-kb + @appian-dev, read-only — NEW/UPDATE tag + the ACTUAL code; **process models node-by-node with code, not summarized**) → synthesize (cross-cutting) → **DETERMINISTIC assemble** (one Lavish-safe `design.html`) → **grounded verify** critic (bounded → escalate) → present → cleanup; reliability trio per agent; recursion_limit 250. **Removed the superseded Phase-15 `design-doc`.** validate_library 12; re-pin genesis v0.62.0 (genesis-core stays v0.9.6)** |
| `genesis-appian-parser` | **v0.2.0** | main | **NEW (Phase 16-01).** Genesis-owned, stdlib-only Appian package parser (port of the Atlas front-half). `parse(zip\|bytes) -> KbParseResult` (objects + edges + bundles + **code-free** metadata; no files, no SAIL). Consumed by `genesis/kb` + the sync workflow; pinned into genesis by tag (**v0.2.0**, 16-03/18-06). **Phase 18 (v0.2.0): dependency-extraction accuracy overhaul — universal known-UUID + raw-XML reference scan, `is_orphan`=disconnected (not unbundled), CDT-QName + translation/document/record-action + `rulereferencebyname` by-name edges, APPREF/ENTRYPOINT cross-app integration-point classification. Measured edge recall 0.32→0.98, precision 0.999, orphans 804→0 on a real app; ≥95% CI gate; 25 tests.** |

**Dependency chain** (git-pinned by tag; CI rewrites ssh→https):
`genesis → genesis-core@v0.9.6 → kiro-agent-sdk@v0.7.1`;
`genesis-workflows → genesis-core@v0.9.2 (runtime) + genesis@v0.44.0 (dev pin)`. (`code-review` needs genesis ≥ v0.20.2 at runtime for the loop.) both genesis + genesis-core pin the SDK directly (v0.7.0 — Phase-21 ACP extensions on top of the Phase-14 `fs_write_root` sandbox). **genesis-core `v0.9.3`** = the managed-native `launch_provider` for **both** MCP (16-08) and CLI (Phase 19, ADR-040) + the introspect stream-limit fix + the Phase-21 SDK pin (v0.7.0) — all additive, `CORE_MAJOR` still 1. **`genesis-appian-parser@v0.2.0`** is a stdlib-only leaf (no Genesis deps); **genesis pins it by tag (16-03)**, so it installs transitively wherever genesis does (incl. genesis-workflows CI). **Phase-19 doc-parsing deps (pinned in genesis):** `pypdf==6.15.0`, `python-docx==1.2.0`, `openpyxl==3.1.5`.

**Tests, all green at last release (as of genesis v0.66.1; ADR-065 collaboration enablement — genesis pytest **781**, web **vitest 262**, genesis-workflows **validate_library 12 + pytest 179**, genesis-core **83**, kiro-agent-sdk **93**):** genesis **665** pytest (Phase-26 the Agentic Memory Layer — memory store/graph/vector/retrieval + `genesis-memory` MCP contract + curation API + scheduler jobs [`test_memory_*`], **web vitest 224** incl. jest-axe on `/memory` + the Feature-Workspace suite (`stages.test.ts` pure fns) + the d3-force constellation render test, **genesis-workflows 93** incl. the `memory-consolidation` + `memory-maintenance` suites; Phase-25 **m0013 `lifecycle_transitions`** + typed domain/`LifecycleService` + spec action API + **m0014 `row_version`** optimistic-lock CAS [`test_optimistic_locking` + `test_run_idempotency`] + structured-logging context + ChatModeProfile posture guards + the god-module-decomposition regressions + **`test_document_provider`** (25-09) + **`test_metrics_api`** (25-13 metrics/provenance/activity); Phase-23 **m0012 `scheduled_jobs`** + scheduler `due_slot`/`ScheduleStore`/tick-engine + `application-sync`/`document-library-sync` handlers + `/system/schedules` + full-package refresh [delta/refresh/auto + 409 guard]; **plus the v0.48.1–v0.48.7 patches: updater dev-guard + whole-stack apply, Dev-MCP `isError`/pagination + `LCP_API_PATH`, SPA-packaging resolver, Phase-24-01 env-scoped creds (+8 `tests/test_env_credentials.py`), Phase-24-02 Applications-first nav/IA + env-dialog viewport fix**; on top of the Distribution/Features/KB/Business-Map/Documents suites; **+ Phase-29 v0.56.x: `test_global_search_*`, run-detail `node-states.test.ts` + `fold_steps` executions, `ReuploadMockupButton` + the StageFinalizer superseded-run guard; + Phase-30: the `technical-design-analysis` workflow tests (19), TD start/guard/finalizer + the `technical_design` chat-mode posture, `deriveAvailability` gating + the now-live-design roll-up**) · genesis-core **80** (incl. managed-native MCP + CLI launch resolution + Phase-25 atomic_json + AgentProvider + reliable_agent_step) · kiro-agent-sdk
**93** · genesis-appian-parser **25** (vs a real vendored package + a no-SAIL guard + a raw-XML accuracy oracle with a ≥95% gate) · genesis-workflows **77** (incl. 13 code-review + 16 design-doc + 14 sync-application + 11 generate-business-map + **7 sync-documents**) + `ci/validate_skills.py` + `ci/validate_library.py` (7 workflows) · web **181** Vitest
(incl. contract-fixture drift + jest-axe a11y + the Phase-19 library/connector tests). ruff clean (**`ruff==0.15.20` pinned in genesis AND genesis-core — see §7**); eslint clean (0 errors); `tsc` strict clean. CI green on all code
repos (genesis has a python `genesis` job + a `frontend` job with a stale-bundle guard **that runs only on `web/**` changes**; the SDK repo
has no CI — validated transitively by core+genesis installing its tag).

**Milestones (see `roadmap-and-sequencing.md`):**
- **Phases 1–6 — DONE (M1–M6):** engine + node framework + state/checkpointer/blackboard + MCP
  registry + reliability trio; workflow contract + library + scaffolder + CI enforcement; distribution;
  config + secrets + env registry + health; run orchestration + all 3 HITL modes + streaming; ERD.
- **Phase 7 WEB REVAMP + Code-Review Fix Program (01–06) — DONE + shipped.** React+TS workbench;
  persistence/migrations, Overview, Integrations Studio (ADR-029), retention, SQLite decision (ADR-030),
  conversation rich-chat.
- **Phase 8 — Settings & Integrations Revamp — SHIPPED (v0.16.0).**
- **Phase 9 — Agent Artifact I/O — SHIPPED** (sdk v0.2.0, core v0.6.0, genesis v0.17.0, workflows v0.4.0):
  a per-run tool-output store + agent `save_tool_output`/`list_tool_outputs` (save-by-reference) so large
  MCP results land in the blackboard with zero re-emission into the context window.
- **Phase 10 — Chat Assistant — SHIPPED** (sdk v0.3.0, core v0.7.0, genesis v0.19.0; ADR-031;
  m0002 migration): a read-only conversational Chat page — talk to Kiro (Atlas read MCP) + query all
  Genesis state via a read-only introspection MCP server (`genesis/mcp/introspection_server.py`).
  Follow-ups: Atlas read `tool_allowlist` (workflows v0.4.2), CRLF SSE streaming fix (v0.19.1), a UI
  polish batch (v0.19.2: breadcrumbs, list-view, collapsed nav, clickable catalog cards). **(The top bar + breadcrumbs
  were later removed — v0.42.0/v0.43.0 — and the theme toggle moved to Settings → General.)**
- **Phase 11 — Credit & Usage Tracking — SHIPPED** (sdk v0.4.0, core v0.8.0, genesis v0.20.0; ADR-032;
  m0003 migration): REAL metered per-turn Kiro credits surfaced everywhere — per agent node + run-total
  (Run Detail), Overview KPI (**Credits Used** replaced Tool-Calls), per chat message + session total.
  Then **v0.20.1**: chat shows the credit count in place of the "Turn complete" chip + a secrets
  atomic-write crash fix (see §7).
- **Phase 12 — Appian Code-Review Workflow — SHIPPED** (genesis v0.20.2 for 12-01; genesis-workflows
  v0.5.3 for 12-02..12-05): a deterministic port of the Jarvis code-review process (Google-Docs export
  excluded). Entry via JIRA ticket / package URL / object names; per-object review loop (diff-aware →
  `analyze_appian_code` → dynamic checklist → SQL/i18n `query_sql` → RecordType relationships) with a
  code-enforced PRE-WRITE CHECKPOINT validator; agent-proposed / program-confirmed verdict. Read-only
  by construction (per-node `@jarvis`/`@jira` allowlists; no registry cap on jarvis). 12-01 added the
  worker loop `recursion_limit` (from `META.execution.recursion_limit`, default 150). `jarvis`+`jira`
  MCP registered + connected. Live run against a real ticket still pending.
- **Backlog:** `specs/backlog/skill-migration-program.md` (45-skill migration; deferred). **Do not start
  backlog/Phase-N work unless asked.**

**What works today (verified):**
- `genesis serve` → FastAPI backend + SPA at `http://127.0.0.1:8760`. API under **`/api`**, Swagger at
  `/docs`, SPA history fallback (ADR-028).
- **Screens (all live):** Overview (metrics incl. **Credits Used** KPI + trend + active runs + integration
  health); Runs list; Run Detail (React Flow graph + inspector + turn-grouped Kiro conversation + all-3
  HITL + documents drawer + **per-node & run-total credits**); Catalog (browse/detail/launch, clickable
  cards); **Chat** (read-only assistant, persisted sessions, per-message credit footer); **Settings**
  (MCP · CLI · GitLab · Environments · General — General holds Appearance/**theme toggle**, Storage, Copilot);
  **Applications** (KB apps — detail tabs Business Map · Overview · Syncs · Releases · **Business Artifacts** [linked documents, Phase 19] · **Features** [Phase 20/21]); the **feature page** (`/applications/:uuid/features/:featureId`) — a Phase-21 **artifact pipeline** (Spec card Edit/View + Design/Breakdown placeholders); the **spec builder** (`…/features/:id/spec`) — a full-width chat + an on-demand full-screen annotatable Preview; **Documents** (the global **Document Library** — list/search/filter, add via upload or Google-Drive link, link/sync/remove, full-screen viewer at `/documents/:id` — Phase 19). **Chat** (main + the spec builder) has Phase-21 **ACP parity** (model selector, `/` command palette + autocomplete, context-usage meter, clear/compact, image attach, Markdown export). Settings→CLI has the **Google Workspace (`gws`) connector card**. Left nav collapsed by default;
  **no top bar / no breadcrumbs** (removed v0.42.0/v0.43.0). **Phase 24-02 (ADR-049, v0.48.6): the primary nav is
  Applications · Chat · Runs · Documents (Applications is the landing — `/` redirects to `/applications`); Overview (metrics)
  and Catalog (Workflows|Skills) moved into Settings as the first two tabs (default Overview).**
- **Data plane:** durable SQLite (`~/.genesis/genesis.db`, WAL) via `genesis/db/` migrations **plus a SEPARATE `~/.genesis/memory.db` (Phase 26, `genesis/memory/migrations/`, `mm0001` — the agentic-memory bi-temporal graph + FTS5 + `sqlite-vec`; `genesis db upgrade` upgrades both DBs)**
  (m0001 runs+events, m0002 chat, m0003 chat_usage, m0004 copilot [chat_sessions.mode +
  chat_run_links + chat_permissions], m0005 supervision [chat_notifications], m0006 copilot_actions [audit], **m0007 kb [the code-free temporal `kb_*` Appian KB — Phase 16-02]**, m0008 business_map [`kb_business_maps` — Phase 17-01], **m0009 documents [`kb_documents`/`kb_document_links`/`kb_document_sections` — the global Document Library, Phase 19]**, m0010 features [`kb_features`/`kb_feature_specs`/`kb_feature_spec_revisions` — per-app Features & Specs, Phase 20], m0011 chat_model [`chat_sessions.model` — per-session LLM, Phase 21], **m0012 scheduled_jobs [the backend scheduler's fixed cadence + last-run, Phase 23]**; m0013 lifecycle_transitions [SDLC audit, Phase 25-01], m0014 row_version [optimistic-lock CAS on kb_features/kb_feature_specs, Phase 25-08], **m0015 feature_stages [`kb_feature_stages`/`kb_feature_stage_revisions` — the generalized per-(feature,stage) artifact model; copies Spec rows in as stage='spec', Phase 29]**; **m0016 stories [`kb_epics`/`kb_stories` + `kb_features.stories_finalized_at` — the finalized backlog, Phase 32]**; **m0017 workbench [`workbench_boards`/`kb_board_cards` — the per-app Kanban execution boards, Phase 33]**; **m0018 story_stages [`kb_story_stages`/`kb_story_stage_revisions` — the per-(story, stage) design artifact, Phase 34]**; **m0019 collab [global `sync_uuid` + `row_version` backfill + owner/team/provenance cols + `collab_identity`/`collab_teams`/`collab_memberships`/`collab_sync_state` — the local-first collaboration substrate, Phase 35]**; `current_version=19`); runs + full conversation +
  checkpoints + chat sessions/messages persist across restart; retention available.
- **Credits (Phase 11):** every agent turn (workflow node OR chat message) reports real credits from
  Kiro's `_kiro.dev/metadata.meteringUsage`; aggregated per-run (`eventlog.aggregate_credits` via
  `json_extract` over `agent.result`) and per-session (`chat_messages.usage`). Provenance
  `metered`/`partial`/`unavailable` — the UI shows honest "n/a", never a fabricated number.
- **Integrations Studio (ADR-029):** curated (MR-governed) + custom (`~/.genesis/mcp-custom.json` /
  `cli-custom.json`) tiers; per-node injection. `jarvis` (read-write-deploy) + `jira` registered.
- `hello-appian` runs GREEN end-to-end; `erd-generation` runs into the live Atlas fetch. All 3 HITL
  modes, streaming, worker isolation, checkpoint resume — implemented + tested.
- **Dev:** `npm run dev` in `genesis/web` → `http://localhost:5173/` (Vite proxies `/api` → :8760).
  Design-system gallery at `/dev`.

---

