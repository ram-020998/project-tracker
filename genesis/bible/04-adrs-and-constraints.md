<!-- GENESIS BIBLE — CHUNK 04. DO NOT summarize or drop content when editing; keep it verbatim-faithful. -->
> **This file is one chunk of the Genesis bible.** The bible is split across `bible/` and indexed by
> [`../AGENT_ONBOARDING.md`](../AGENT_ONBOARDING.md). **When asked to "read the bible", read the index AND every
> chunk it lists, then follow all of it religiously.** This chunk holds: **§5 Non-negotiable constraints — ADR-001..047 + key implementation contracts.**
> Section numbers (§0–§10) are the ORIGINAL bible sections and are preserved here; the §→chunk map lives in the index.

---

## 5. Non-negotiable constraints (ADRs — do not violate; flag + confirm if a task requires it)

- **ADR-001** — LangGraph owns control flow; agents never orchestrate.
- **ADR-002/004/020** — agent steps = narrow Kiro turns over ACP, **per-node MCP injection**; NO global Kiro `mcp.json`.
- **ADR-005 (+ ADR-029)** — the shared MCP/CLI registry lives in the library (curated tier, changed via MR). ADR-029 adds a **user-writable custom tier** on top; the curated tier stays MR-governed. Effective tool trust = `node.tools ∩ server.allowlist` (a hard cap).
- **ADR-010/018/022** — state is small/serializable/**editable** (pointers + decisions); bulk lives in the `RunWorkspace` blackboard under `$GENESIS_ARTIFACTS_DIR`; never inline bulk in state/chat.
- **ADR-011** — reliability trio (validator + retry + escalation) MANDATORY on every agent node; CI-enforced.
- **ADR-012** — graph execution runs in a disposable **subprocess worker**, not the app; pause=kill, resume=fresh worker from checkpoint; the app never imports workflow Python.
- **ADR-019** — genesis-core semver; `CORE_MAJOR` (=1, distinct from pip version) is the compat-gate key; the loader refuses a library whose declared `genesis_core_major != CORE_MAJOR` (additive-only within a major).
- **ADR-021** — `META.auto_approve=true` default; pause only at 3 sanctioned gate classes (approval|escalation|pre_mutation); `pre_mutation` REQUIRED before any write/deploy/data MCP node.
- **ADR-023** — backend is FastAPI **embedding** LangGraph (NOT a LangGraph Server); Studio is interim debug only.
- **ADR-024** — engine is **async-first** (ainvoke/astream + AsyncSqliteSaver + aiosqlite); Python pinned 3.13.
- **ADR-025** — "fork" = seed a NEW thread from a past checkpoint; the original is untouched.
- **ADR-026** — web UI = React+TS (local single-user); multi-user/hosted would re-open ADR-012/023 (separate track). **(Amended Phase 25-04:** the local-single-user posture is now guarded at the edge — `genesis serve`/`up` **refuse a non-loopback bind** unless the operator explicitly opts in with `--i-understand-no-auth` / `GENESIS_ALLOW_NON_LOOPBACK=1`, since Genesis ships **no authentication**. Binding to `0.0.0.0` without auth would expose an unauthenticated app on the network.**)**
- **ADR-027** — the web stack (Tailwind + shadcn/Radix + React Router + TanStack Query + React Flow + react-hook-form/zod + react-markdown/mermaid/shiki + Recharts + Vitest/RTL/MSW) and the Overcut visual language (07-03a). **Reuse existing primitives; use design tokens, not raw palette colors.** (Stack extensions, additive: **d3-force** for the `/memory` constellation [v0.52.1]; **elkjs** for the run-detail workflow-graph layered layout + orthogonal edge routing [v0.58.0].)
- **ADR-028** — ALL backend endpoints under `/api` (APIRouter) + SPA history fallback; the web client prepends `/api` centrally (never hard-code it in components); the Vite dev proxy is a single `/api` → :8760.
- **ADR-029** — two-tier MCP/CLI registry (curated read-only + custom writable) + tool allowlist + direct-stdio introspection.
- **ADR-030** — persistence stays **SQLite**; move to Postgres/pgvector only on an explicit trigger (multi-user, transcript RAG, or heavy analytics). Repositories keep DB-agnostic signatures so the seam stays cheap.
- **ADR-031** — **Chat is a read-only assistant** (never orchestrates): it observes/answers, never drives or mutates. Enforced (defense in depth): `trust_tools` allowlist of read tools only + SDK `permission_mode="auto_deny"` + `allow_fs_write=False` + a read-only `genesis.db` connection in the introspection server. kiro-cli matches MCP tools by the **namespaced** `@server/tool` name — build allowlists as `@server/<tool>`. Chat runs in-process (no subprocess worker; ADR-012 is about workflow Python). **(ADR-034 refines this: since Phase 14 chat runs `allow_fs_write=True` but SANDBOXED via the SDK `fs_write_root` to the per-session skill-output dir only — a skill may write documents there, nothing else on disk.)**
- **ADR-032** — **credit usage is REAL metered data from Kiro ACP** (`_kiro.dev/metadata.meteringUsage`, verified per-turn not cumulative), NOT estimated — there is no pricing engine. SDK captures it → telemetry + `agent.result` + `run_events`/`chat_messages.usage`. Every figure carries `provenance` (`metered`/`partial`/`unavailable`); the UI shows honest "n/a", never a fabricated number.
- **ADR-033 (ACCEPTED — Phase 13, SHIPPED)** — the **Chat copilot may operate runs** (start / read status / answer gates / cancel) at the **run-management layer only**, but (a) LangGraph still owns each workflow's control flow (ADR-001 intact — the copilot calls the same `RunManager` API a human clicks, it is NOT the workflow engine), (b) **every mutation is human-confirmed** (launch dialog for `start_run`; per-call confirm card for `respond_to_gate`/`cancel_run`, via the untrusted-tool → `session/request_permission` → SDK `permission_mode="ask"` bridge), (c) it can NEVER auto-approve/bypass a workflow's own HITL gate — only relay the human's decision, (d) NO config/secret/registry/workflow-definition/deploy tools, (e) read-only default + global kill-switch + per-session concurrency/rate/allow-deny limits (13-06, enforced app-side on `POST /api/runs` gated on the control token) + a `copilot_actions` audit trail. Refines ADR-031; preserves ADR-001. Delivered 13-01..13-06 (genesis v0.25.0 + sdk v0.5.0).
- **ADR-034 (ACCEPTED — Phase 14, SHIPPED)** — **Skills are a first-class "standalone activity" concept beside Workflows, chat-invoked + filesystem-provisioned.** A **Skill** = a single standalone activity (draft a document, build a checklist, apply a body of knowledge like GAM) owned by the **Kiro agent** (its `SKILL.md`), no stages/run; a **Workflow** = a staged/orchestrated activity owned by **LangGraph** (ADR-001 preserved — a skill never starts a run). Skills are **filesystem-discovered** (NOT an ACP wire param like MCP): Genesis provisions them into a managed Kiro workspace **`~/.genesis/.kiro/skills/`** (= the chat `cwd`; **spike-proven** over ACP — auto-activation by `description` + explicit `/skill-name`). Two acquisition paths: install from a new `skills/` library in `genesis-workflows` (mirrors the workflow install/lockfile path) OR author in-flight (SKILL.md + scripts/references/assets). Catalog gains **Workflows | Skills** sub-tabs; Chat's `/` palette becomes a unified workflows+skills menu (skills also work in read-only chat). **Safety:** a skill may write documents **only** into a per-session **skill-output sandbox** `~/.genesis/skill-output/<session_id>/` (via a small additive SDK **`fs_write_root`** option — writes elsewhere rejected); executing bundled `scripts/` stays deferred. Refines ADR-031/033 (chat gains bounded sandboxed document output, no other write authority). Delivered 14-01..14-05 (kiro-agent-sdk v0.6.0 + genesis-workflows v0.6.0 + genesis v0.26.1).
- **ADR-035 (ACCEPTED — Phase 15, SHIPPED)** — **Run input file attachments.** A workflow input may declare `format:"file"`. Such inputs are provisioned at launch, not passed inline: a new **multipart `POST /api/runs/upload`** (browser-only; the JSON `POST /api/runs` is unchanged for tokened/copilot starts) + **`RunManager.start(..., files=)` / `_provision_files`** writes each upload into the run **blackboard** under `uploads/<sanitized>` and rewrites the matching input to that blackboard-relative path **before** schema validation. Guards: 10 MB cap, extension allowlist (`.txt .md .html .htm .csv .png .jpg .jpeg`), filename sanitization (no traversal), target must be a declared `format:"file"` prop (`FileUploadError` → 400). The worker just reads a file already in its own workspace — **ADR-012 isolation intact**; uploads are **read-only inputs, never executed** (mirrors Phase-14 `scripts/` posture). Web: the 07-05 launch form renders a `FileDropList` for file inputs and submits multipart. Preserves ADR-010/018 (bulk → blackboard, never inline). Enables the Phase-15 mockup→i18n branch. Delivered 15-01 (genesis v0.27.0).
- **ADR-036 (ACCEPTED — Phase 16; shipped 16-02/03/04, Atlas cutover in 16-05)** — **Internalized Appian Knowledge Base.** Genesis owns the Appian KB: a Genesis-native parser + a local KB in `genesis.db`, fed by the **single dev-tagged environment** (the Environments registry may hold many; a single-select **`is_dev` toggle** designates the one Phase 16 authenticates against — URL + creds for REST export + Dev/DevOps MCP; Deployment-REST export + Dev MCP). External **Atlas MCP** (GitLab-served) and **Jarvis** are **retired as the KB source** (Atlas = inspiration + interim source until the 16-05 cutover). Sync is a **deterministic LangGraph workflow** (program-node REST export, no agent → ADR-001 preserved). Aligns with local single-user / one-env / own-data-plane (ADR-023/026/030).
- **ADR-037 (ACCEPTED — Phase 16; code-free `kb_*` shipped 16-02/03, live code wires in 16-05, versioning = 16-06 backlog)** — **Code-free temporal KB + live code via the Dev MCP.** The KB stores **only** metadata/structure/dependency-graph/bundles — **never** SAIL source. All code (current + historical) is fetched **live** via the **Dev MCP** (version-parameterized). Object history = a **temporal SCD-2** model keyed to syncs; **user-tagged releases** (`kb_releases`) name points in time; `env_version_ref` bridges a release to the env version. Refines ADR-030 (SQLite `kb_*`; semantic search over parsed content = future pgvector trigger) + ADR-010/018 (export zip + parser output → blackboard; only metadata → `kb_*`). Historical-code slice depends on Dev MCP **AP-62096** (26.8 GA).
- **ADR-038 (ACCEPTED — Phase 16, SHIPPED)** — **Managed native Appian MCP servers (vendored, versioned, replaceable, not forked).** The **Dev MCP** (`lcp-mcp-server`) + **DevOps MCP** (`appian-deployment-mcp`) are installed as managed, versioned local servers (`~/.genesis/mcp-servers/<id>/versions/<v>/` via `uv sync`; launched from the per-server venv; **read-only allowlists**; registered as a **managed reference**, not a static image → resolves the old `lcp` `<lcp-image>` placeholder). **Updatable without forking = MANUAL drop-in (2026-08-05 decision):** a new Appian release is integrated by the operator dropping the new bundle in → Genesis installs it as a new version + swaps `current` (prior kept for **rollback**; sha-verified; bundle never modified). **No auto-fetch update source** — the earlier connected-site bundle-servlet (Dev) and configured-mirror (DevOps) fetch were dropped. New prereq: `uv` at install time; Dev-MCP Basic auth is the headless default (browser/SSO = opt-in). **§2.0 (dev-env `is_dev` toggle) shipped (genesis v0.30.0); Stage B (the managed-native installer + managed-ref resolution + registry entries + API/CLI/web panel) SHIPPED — genesis-core v0.9.1 + genesis v0.31.1 + genesis-workflows v0.8.4; 16-08 COMPLETE.**
- **ADR-040 (ACCEPTED — Phase 19, SHIPPED)** — **Managed-native CLI connector (`gws`).** The Google Workspace CLI is installed as a managed, versioned local **binary** (`~/.genesis/cli-tools/gws/versions/<v>/`; the CLI analog of ADR-038's native MCP — a `genesis-core` `CliRegistry` `"managed":"<id>"` entry resolves via an injected `launch_provider`). Genesis owns an **isolated** config dir (`~/.genesis/cli-tools/gws/config`, file keyring, its own `gws auth login`) and **reads the OAuth client** from the dotfiles-provisioned `~/.config/gws/client_secret.json` — **ships no token**; dotfiles setup is a documented prerequisite. **Read-only** Drive/Docs/Sheets/Slides scopes only (allowlist enforced before spawn). Never point Genesis at the user's real `~/.config/gws` (a forced-keyring probe once invalidated their creds). genesis-core **v0.9.2** + genesis **v0.44.0** + genesis-workflows **v0.9.3**.
- **ADR-041 (ACCEPTED — Phase 19, SHIPPED)** — **Documents are a global first-class store linked into applications.** One row per **unique** document (dedup by `gdrive:<fileId>` / `upload:<sha256>`), stored **once** on disk (**latest version only** — sync overwrites), **linked** into ≥1 app via `kb_document_links`. **Untracking an app removes its links, never the shared document** (FK `ON DELETE CASCADE` covers links). Code-free metadata + pointers + a change-detection fingerprint in `genesis.db` (m0009); bulk parsed Markdown/JSON on disk (ADR-010/018). Refines ADR-030 (semantic/pgvector doc search = a future trigger). genesis **v0.44.0** (+ the `sync-documents` workflow in genesis-workflows **v0.9.3**).
- **ADR-042 (ACCEPTED — Phase 20, SHIPPED)** — **Features & Specs are first-class sub-entities of an application; a spec is authored conversationally (Chat, not a workflow), HTML-authoritative.** m0010 (`kb_features` FK→`kb_applications ON DELETE CASCADE`, so untracking an app cascade-deletes its features → specs → revisions — **intrinsic to the app**, unlike Phase-19 documents). A spec's authoring surface is a **reused Chat session** (ADR-031/034 lineage) — **LangGraph is not involved, ADR-001 intact**; `genesis-kb` (16-05) gives the agent the app's KB. The **HTML artifact is authoritative** (Markdown is a derived export); the agent writes `spec.html` into its per-session `fs_write_root` sandbox (Phase 14, no new write authority). Lifecycle `draft → in-progress → in-review → completed` (user-set; agent may suggest) + milestone snapshots. **One spec per feature** in v1. genesis **v0.45.0**.
- **ADR-043 (ACCEPTED — Phase 20, SHIPPED)** — **Embed the Lavish annotation SDK (vendored, MIT) for in-app HTML review.** Vendor the browser SDK of `kunchenguid/lavish-axi` (`artifact-sdk.js` + `mermaid-node.js` @ `899747a`) into `genesis/api/assets/lavish/`, Genesis-theme it (its `:host` palette → `--lavish-*` overrides fed from design tokens), and host the spec artifact in a **same-origin sandboxed `<iframe>`** whose host is our React chrome listening on `postMessage`. Genesis does **not** run Lavish's server/CLI/poll/export/ht-ml.app sharing. Rationale: the SDK makes **no** server calls (`parent.postMessage` only), fits the single-SPA UX + in-process ChatManager, and avoids a second window / unauthenticated `:4387` server / Node-≥22 runtime. Upstream tracked manually (pinned commit + a documented `:host` theming patch); attribution in `THIRD-PARTY-NOTICES`. Mermaid-whiteboard deferred. genesis **v0.45.0**.
- **ADR-044 (ACCEPTED — Phase 21, SHIPPED)** — **A Feature is a workspace of sequential artifact stages.** The feature page is a **pipeline of artifact cards** (Spec → Design → Breakdown → …), each artifact carrying **its own** status; the **feature has no status derived from any single artifact** (feature-level status is a later, separate concept). Artifacts open in two modes: **Edit** (author — e.g. the spec builder at `…/features/:id/spec`) and **View** (read-only preview — the spec artifact route with `annotate=0`, no Lavish SDK). Landing on a feature shows the pipeline, not the builder; the Features-tab card no longer shows the spec's status. Sequential unlock-on-completion is part of the model but **deferred** until ≥2 artifacts exist (Design/Breakdown ship as disabled placeholders). genesis **v0.46.0**.
- **ADR-045 (ACCEPTED — Phase 21, SHIPPED; refines ADR-031)** — **The reused chat mirrors the Kiro CLI/ACP surface.** Genesis adopts the ACP extensions (`session/set_model` + the models advertised on `session/new`, `_kiro.dev/commands/{available,options,execute}`, `_kiro.dev/{compaction,clear}/status`, image prompts) so the in-app chat offers model selection, slash commands, context/compaction views, clear/compact, and image attachments — in **both** the main chat and the spec builder. This **relaxes ADR-031's "Chat is read-only"**: the surface is **no longer categorically read-only**, but **write-capable actions stay human-confirmed** via the existing `permission_mode="ask"` bridge (ADR-033) — not blanket-denied; safe introspection commands run freely. Model choice is **at creation** (`m0011 chat_sessions.model`); mid-conversation switch deferred. Bounded by local single-user (ADR-026). Needs **kiro-agent-sdk v0.7.0** (additive ACP methods; verified vs kiro-cli 2.16.2 in the 21-01 spike — autocomplete is client-side since the per-command `optionsMethod` isn't wired in 2.16.2; `execute` streams, with the prompt-path as fallback). genesis **v0.46.0** + genesis-core **v0.9.3** (SDK pin).
- **ADR-046 (ACCEPTED — Phase 22, SHIPPED)** — **Genesis ships as a local, browser-based app via clone + git-tag self-update.** Distribution = clone the `genesis` repo + venv + `pip install .` (the three internal deps resolve from their `git+ssh` tag pins → **one clone**; `genesis-workflows` is pulled at runtime), launched by **`genesis up`** which backgrounds `genesis serve` and opens the default browser at `http://127.0.0.1:8760`, and updated in place by checking out release tags (`git fetch --tags` → on-branch guard → checkout `vX.Y.Z` → `pip install .` → `genesis db upgrade` → detached restart). **Not** a native desktop app (dropped Friday's Swift `.app` — browser-only), **not** Docker, **not** hosted. Modeled on `appian/prod/friday`'s clone+tag installer, adapted to Genesis's single-port `serve` + single-user/localhost design (ADR-026). Reuses existing pieces (`scripts/genesisctl.sh` → now a thin wrapper over the CLI launcher, `genesis db upgrade`, committed `web/static`, `/api/config/health`) and the SSH access users already have — **no package index or per-user token**. New runtime code is a small read-only-ish **`api/system`** surface (update-check/apply, kiro status/login/logout, preflight) + `runtime/{launcher,updater,kiro_auth,preflight}.py` + web `features/system` (UpdateBanner + PreflightChecklist) + Settings Kiro sign-in. **A wheel + package-index transport is a deferred phase-2 alternative** (only if the git-clone requirement must go); the launcher/updater/preflight/Kiro-login are transport-agnostic. Preserves ADR-031/045: the `api/system` surface is local introspection + **local** process/install control only (never shared systems). genesis-only, no schema. genesis **v0.47.0**.
- **ADR-047 (ACCEPTED — Phase 23, SHIPPED)** — **Genesis runs fixed, backend-defined scheduled maintenance syncs, and the app sync is re-runnable as a full-package refresh.** (1) The `sync-application` **`mode=delta`** (16-07 Option A) — a **full re-export → parse → diff the DB by `diff_hash` → write only the changes** — is unblocked at the API (`_resolve_mode`: auto-pick baseline↔delta; `refresh` alias) with a per-app **already-running 409** guard (the Appian Deployment export is one-at-a-time) + a web **Refresh** action. It is **not** an environment delta-patch (true incremental stays deferred — no Appian changed-objects API — and is unnecessary for daily freshness). (2) A lightweight in-process **asyncio scheduler** (`runtime/scheduler.py`, started with `genesis serve`) ticks each minute and fires due jobs as their own tasks: **`application-sync`** (all tracked apps, weekday **07:00 IST**, **serialized**, per-app mode-pick, skip-if-running, skip if no dev env) + **`document-library-sync`** (weekday **08:00/12:00/16:00/20:00 IST**, one `sync-documents` `scope=library` run, skip if `gws` down). Schedule + last-run live in **m0012 `scheduled_jobs`** (backend-fixed now, **user-configurable later** — a read-only `GET /api/system/schedules` is the only surface this phase). TZ-aware (IST), weekdays only, daytime slots, **restart-safe** (a persisted `last_fired_slot` → within-day catch-up, no cross-day re-fire). Jobs are **read-only against Appian** + local KB/document writes, **`auto_approve`, no HITL gates**, calling the same `RunManager.start` a human clicks — preserving **ADR-001** and extending the **ADR-033** "operator at the run-management layer" posture to a **non-interactive** actor; bounded by local single-user (ADR-026). genesis-only + m0012. genesis **v0.48.0**.
- **ADR-048 (ACCEPTED — Phase 24-01, SHIPPED)** — **Core Appian-MCP credentials are environment-scoped.** For the two core MCPs ONLY (`appian-dev`/`appian-devops`), credentials (`LCP_USERNAME`/`LCP_PASSWORD`/`APPIAN_API_KEY`) are entered on the **environment** form and stored per-environment in the SecretProvider under a grammar-safe **`env-<sha1(label)[:16]>`** scope (never in `environments.json`); they are resolved **only** from the **dev-tagged** env via `EnvironmentRegistry.resolve_var` (the one seam worker/chat/cards share — secrets injected into the registry at every construction site). `LCP_API_PATH` becomes a **public** env field; `LCP_URL`/`APPIAN_DOMAIN` stay **auto-derived** from the env `url` and are no longer surfaced. The two servers no longer render an MCP config card (`fields.ENV_MANAGED_SERVERS`) — a detail-page pointer sends users to Settings → Environments. **Invariant preserved:** true secrets stay in the SecretProvider (0600 + atomic). **Back-compat:** a one-time non-destructive migration (`ConfigService._migrate_legacy_core_secrets`) relocates legacy server-scoped creds into the dev env scope (originals deleted so edits take effect) — existing users re-enter nothing. Every other MCP keeps its own card. API exposes only `is_set` booleans for creds, never values. genesis-core untouched (a genesis-side SecretProvider adapter is unnecessary — the resolve_var seam suffices). genesis **v0.48.5** + genesis-workflows **v0.9.5** (secretKeys/publicKeys emptied for the two servers).
- **ADR-049 (ACCEPTED — Phase 24-02, SHIPPED)** — **Applications-first IA.** The primary sidebar is reduced to **Applications · Chat · Runs · Documents**; **Applications is the landing** (index `/` redirects to `/applications`). **Overview** (metrics) + **Catalog** (Workflows|Skills) move into the **Settings** workspace as the first two tabs (two zones: Workspace = Overview/Catalog, then Configuration = MCP/CLI/GitLab/Environments/General), with **Overview the default Settings tab**. Reusable `OverviewSection`/`CatalogSection` (no `Page` chrome) are embedded; the standalone `/catalog[/:workflowId/launch]` routes + `OverviewPage` are retained for deep links (the Copilot launch flow uses them). Container kept named "Settings" (Option A); a later rename to "Manage"/"System" (Option B) is a noted optional follow-up. Frontend-only; genesis **v0.48.6**. **(Amended Phase 27-11, shipped in v0.53.0):** the landing is now a **Home dashboard** — the "Applications" nav item is **relabeled "Home"** (route stays `/applications`), and the **at-a-glance metrics + run trend move from Settings→Overview onto the Home page** (above the app grid). The Settings **Overview tab is removed**; the remaining `MetricsSection` becomes a lean **"Metrics"** tab and **Catalog is the default Settings tab**. `OverviewPage`/`/overview` retained for deep links. **(Refined same day:** Settings is now **configuration-only** — Catalog + Metrics tabs also removed; Catalog via its nav, `MetricsSection` relocated to the Home dashboard; Settings = MCP · CLI · GitLab · Environments · General, default MCP.**)**
- **ADR-050 (ACCEPTED — Phase 25-01, SHIPPED genesis v0.49.0)** — **Typed SDLC domain + single-authority `LifecycleService`.** SDLC lifecycle state is modeled as typed domain objects (`genesis/domain/`: `enums`/`entities`/`transitions`/`lifecycle`/`events`/`errors`) and **every** status change goes through one authority — `LifecycleService.transition(kind, id, action)` — which looks the `(state, action)` pair up in a declarative transition table (any→any is impossible by construction: a missing pair is `IllegalTransitionError`; a legal pair with an unmet precondition is `PreconditionFailedError`), persists via injected store accessors, and emits a `LifecycleEvent` recorded to **m0013 `lifecycle_transitions`** (append-only audit, via `LifecycleAuditStore`). The API surface is action-based: `POST /features/{id}/spec/actions/{action}` + `GET .../spec/allowed` (illegal/precondition → **409** with the allowed-actions list); the legacy any-status `PATCH .../spec/status` is deprecated (Deprecation header), and the web replaces its status `<select>` with allowed-action buttons. The service is store-agnostic + unit-testable without a DB (fake stores). Supersedes the previous last-write-wins status setter as the front-door; `FeatureStore.set_status` remains the persistence primitive the service writes through. Extended by ADR (25-08) with a state-CAS so concurrent transitions can't both win.
- **ADR-051 (ACCEPTED — Phase 25-05, SHIPPED genesis-core v0.9.4)** — **`AgentProvider` interface over the agent runtime.** The workflow-agent turn path depends on an `AgentProvider` Protocol (`genesis_core/agents/`) rather than binding `nodes/agent` directly to the Kiro/ACP loader; `KiroAcpProvider` is the default implementation wrapping the existing `_load_real` resolver, injected via `get/set_agent_provider`. Behavior-preserving (the `set_collect_impl` test seam still works); it makes the agent runtime swappable/mocked and is the seam a future non-Kiro provider or a chat session-provider plugs into (the chat session-provider is deferred). Additive; `CORE_MAJOR` unchanged. (Its sibling **ADR-052 DocumentProvider** remains **Proposed** — 25-09, not yet built.)
- **ADR-052 (ACCEPTED — Phase 25-09, SHIPPED genesis v0.50.0)** — **`DocumentProvider` interface for document sourcing.** Document sourcing (resolve metadata + change-fingerprint, fetch bytes) sits behind a thin Protocol (`genesis/integrations/documents/`: `DocumentProvider` + provider-neutral `DocRef`/`DocMeta` + `DocumentProviderError`), with `GoogleDriveProvider` as the first implementation (wrapping the read-only `gws` connector). `DocumentSyncEngine` depends on the interface (not `GwsClient`), and `build_document_providers()` is the composition-root registry keyed by source type — so a second source (SharePoint) is an **additive adapter + one registry entry**, not a branch through business logic (review §F/§19/§20). The fingerprint (Drive revision/md5) drives stale-detection (§21). **Constrained scope:** interfaces only where a 2nd implementation is plausibly on the roadmap — documents qualifies; the **Appian Deployment REST export stays concrete** (one impl — avoids review §36 over-abstraction). Uploads remain a built-in local path.
- **ADR-053 (ACCEPTED — Phase 26, SHIPPED genesis v0.52.0 + genesis-workflows v0.10.0)** — **The Genesis Agentic Memory Layer.** A persistent, self-maintaining memory distilled from the conversations Genesis already stores, so agents get more effective over time. **Two scopes** — **personal** (a *named user's* preferences/instructions/habits — never shared) vs **shared** (an application's knowledge/architecture/decisions — **entity-anchored** with **traversable relationships**) — × **three types** (semantic/episodic/procedural), **bi-temporal** (invalidate, never overwrite; history preserved). A nightly **`memory-consolidation`** workflow distils chats (extract → reconcile ADD/UPDATE/INVALIDATE/NOOP → embed); a periodic **`memory-maintenance`** ("dreaming") workflow reflects/dedups/decays/recomputes communities. Injection is **MCP-only** — a **read-only `genesis-memory` MCP** (hybrid retrieval: semantic + keyword + entity/graph × recency × importance) wired into chat + steered; **no auto pre-fetch**. **Human curation is authoritative** — a browser-only curation API + `/memory` web workspace (list/graph/inspector/review); edits are bi-temporal and **exempt from the maintenance job** (pinned/protected/user_verified/`origin='user'`). The agent MCP stays strictly read-only (ADR-031/045 intact). **Deferred (seams built):** multi-user ACL, auto-prefetch, hard-delete/purge, agentic-node MCP injection.
- **ADR-054 (ACCEPTED — Phase 26, SHIPPED genesis v0.52.0)** — **Memory store/infra + local embedder.** A **NEW, separate `~/.genesis/memory.db`** (genesis.db untouched at v14) with its own migration set (`mm0001`): a bi-temporal entity-relationship graph (`memory_entities`/`memory_relationships`/`memories`/`memory_entity_links`/`memory_links`/`memory_communities` + a consolidation cursor) + **FTS5** keyword index + a **`sqlite-vec`** vector index, all behind **DB-agnostic seams** (`MemoryStore` + `GraphStore` [recursive-CTE traversal] + `VectorIndex` + `Embedder`) so a later **Postgres + pgvector (+ Apache AGE)** swap is a re-home, not a rewrite. Embeddings come from a **small LOCAL embedder** (default `model2vec` static, optional extra; `fastembed` alt) loaded **only in the worker/MCP subprocess** (never the always-on web process) and degrading to a `NullEmbedder` (FTS+graph only) when absent — honoring the RAM constraint + ADR-026 (no outbound data). `genesis db upgrade` upgrades both DBs.

- **ADR-055 (ACCEPTED — Phase 27, SHIPPED genesis v0.53.0)** — **UI/UX design language + light-first theming.** Modernize the whole web UI to a **light-first** (light = default; dark first-class + toggleable) **MUI-*inspired*** language by **evolving** the existing token + Tailwind + shadcn-style primitives (**NOT** adopting `@mui/material`); palette **Indigo `#4f46e5` + Slate `#475569`**; **single-source theming** — two `:root` brand knobs drive both themes + gradients/glows via `color-mix` (no hardcoded brand hex in components); **Poppins** self-hosted. Frontend-only; presentation/IA only (no API/DB/behavioural change).
- **ADR-056 (ACCEPTED — Phase 28) — The Genesis Feature Workspace (parallel, plug-in stages).** A Feature is a workspace of **parallel, independently-advanceable, plug-in stage containers** (Spec, UX Design, Technical Design, Feature Breakdown) over a shared artifact / version-provenance / activity / lifecycle substrate; each stage = its **own `LifecycleService` machine** (ADR-050; m0013) addressed per **(feature, stage)**. UX = command-center **Overview + peer stage cards** (non-gating progress; **no** stepper); opening a stage → a **full-bleed stage workspace** (own URL) with **Expand → immersive**. Feature status is **derived** (roll-up) + explicit Blocked/Cancelled override. **Single-user** (no assignment/roles/My-Work; ADR-026). **Read-only-now** — story execution / deploy / git are **reserved plug-points** (future program, own ADRs; ADR-021/033). **Stories** reserved as a first-class slot for a later phase. **Supersedes ADR-044's sequential unlock clause** (feature-as-workspace core stands). Reuses ADR-055 + Phase-20/21 authoring; a small additive migration (`m0015`) is allowed. **Built 28-04; independent review 28-05 = SHIP; ships at 28-06.**
- **ADR-057 (ACCEPTED — Phase 29) — The UX Design stage: grounded mockup→implementation analysis.** A Feature's **UX Design** stage ingests an uploaded **PDF** mockup and produces a **"UX Implementation Analysis"** HTML artifact via a deterministic **`ux-design-analysis`** workflow (program render + narrow **multimodal** Kiro agent nodes under the reliability trio + a **grounded verification** critic), then a bound **`ux_design` completion chat** finalizes it. Grounded per-screen in the mockup images + the feature's **Spec** + the **live env** (structure via **genesis-kb**, actual code via **appian-dev**), at **intent level** (not object-level design). Enablers: additive **image support in `kiro_node`** (SDK already supports it, gated on `promptCapabilities.image`); **PyMuPDF** PDF→PNG (PDF-only; PPTX deferred); the ADR-056-reserved **`m0015`** generalized per-`(feature, stage)` artifact model + a `ux_design` lifecycle machine (`current_version` → 15); UX becomes the **first `available:true` stage after Spec** (STAGE_DEFS row + registry entry + inner workspace, no shell edits). Read-only against Appian (ADR-036/037); reuses ADR-011/031/035/042/043/045/050. Multi-repo (core → genesis → genesis-workflows). **Reuses the Spec-page UX by GENERALIZING its components** (SpecWorkspace→StageArtifactWorkspace, PreviewDialog→AnnotatablePreviewDialog, SpecBuilderPage→StageBuilderPage, reused ChatThread, stage-scoped hooks) — not new-built look-alikes; Spec keeps working. **Spec repoints FULLY onto the m0015 StageStore (decision A, 2026-08-28) — data-safe additive migration; `kb_feature_specs` kept as a dead table for rollback.** **Drafted 29-01, finalized 29-03, built + Accepted at 29-04; released 29-06 (kiro-agent-sdk v0.7.1 · genesis-core v0.9.6 · genesis v0.55.0 · genesis-workflows v0.12.0; CI green).**
- **ADR-058 (ACCEPTED — Phase 30, SHIPPED genesis v0.57.0 + genesis-workflows v0.13.0)** — **The Technical Design stage: grounded, workstream-decomposed technical design.** A Feature's **Technical Design** stage consumes the finalized **Spec** + **UX Implementation Analysis** (+ an optional comment) and produces a reader-first, **object-level, code-grounded** Technical Design HTML via a deterministic **`technical-design-analysis`** workflow (ADR-001): `plan_sections` (decompose into **functional workstreams**) → a per-workstream **existing-state grounding** loop (`analyze_section`, @genesis-kb structure + @appian-dev code, read-only) → a per-workstream **design drafting** loop (`draft_section`) → **`assemble`** (an agent coherence pass) → a **grounded `verify`** critic (bounded revise → escalation gate) → `present`; each agent wears the ADR-011 reliability trio; two queue-pop loops reset the loop node's retry counter per iteration. Every change names a real existing object or is marked **NEW**; the agent must not assume — blind spots become Open Questions (the inverse of ADR-057's intent-level UX doc). A bound **`technical_design` completion chat** (a `ChatModeProfile` + `_STEERING_TD` — the only new agent prompt) finalizes it via the same annotatable review. **Amends ADR-056** — a stage MAY declare **prerequisite stages** (`StageDescriptor.requires`): TD requires the Spec + UX artifacts present (in-review/completed), enforced **frontend** (`deriveAvailability` → locked card/blocked workspace) **and backend** (start 409 + `resolve_inputs` fail-fast). **Read-only** against Appian (ADR-036/037; the completion chat's only write authority is its fs sandbox). **Reuses the Phase-29 surface** (m0015 StageStore, `StageArtifactWorkspace`/`AnnotatablePreviewDialog`/`StageBuilderPage`, the StageFinalizer — generalized to a workflow→stage `_BINDINGS` registry serving both workflows). **No migration** (`current_version` stays 15); **genesis + genesis-workflows only** (genesis-core/kiro-agent-sdk unchanged). Two UX refinements shipped with it: the whole **stage card is clickable** (no Open button; locked/not-available cards not navigable) + **artifacts are openable** (generated → read-only preview; reference → the Document Library viewer). Reuses ADR-011/031/035/042/043/045/050. Mirror in `reference/decision-log.md`.


**Key implementation contracts:**
- Node fns are `async fn(state, config: RunnableConfig)`. LangGraph injects `config` only when the param is annotated `RunnableConfig`; nodes read ctx via `ctx_from_config(config)`.
- `build(ctx) -> CompiledStateGraph` compiled with `ctx.checkpointer`. `META` is a static dict importable with NO side effects. `workflow.yaml` MUST mirror `META` (parity lint; UI-only keys like `graph:` are exempt via `YAML_ONLY_KEYS`).
- Workflow `graph.py` is loaded standalone (importlib on the file) — keep it self-contained (no sibling-package imports); put pure functions in `graph.py`.
- ACP MCP entries: `env` is a **LIST** of `{name,value}` (NOT a dict). See §7.

---

